# notes/ 챕터 검토 결과 및 수정 계획

- 검토일: 2026-08-04
- 대상: `notes/ch01.qmd` ~ `notes/ch20.qmd` (총 5,686줄)
- 방법: 전 챕터 정독 + 수치 예제 R 4.6.1(renv) 재계산 + 렌더링 실증 테스트(현 설치본 Quarto/pandoc)
- 참고: 현재 출력은 HTML(website, MathJax)만 활성화. `_quarto.yml`의 PDF(xelatex) 설정은 주석 상태.
  → "PDF 위험"으로 표시된 항목은 지금은 통과하지만 PDF를 되살리는 순간 깨지는 것들.

## 우선순위 정의

- **P0** — 지금도 잘못 렌더링되거나 수학적으로 틀린 수식/코드. 즉시 수정.
- **P1** — 내용(사실·개념·수치) 오류. 렌더링은 되지만 틀린 정보.
- **P2** — 표기·용어 일관성, 문장 다듬기. 일괄 치환 가능한 것 다수.

## 전체 판정 요약

| 챕터 | 주제 | 상태 | 핵심 문제 |
|------|------|------|-----------|
| ch01 | 통계학 소개 | 양호 | 머니볼 연도 등 사실 오류 소수 |
| ch02 | R 입문 | 가장 견고 | 표기 통일 위주 |
| ch03 | 데이터 수집 | **내용 수정 시급** | 편향 분류 오류, 1948년 선거 역사 오류 |
| ch04 | R 데이터 추출 | 양호 | 메르센 트위스터 수식, seed 오역 |
| ch05 | 데이터 불러오기 | 주의 | MASS/tidyverse 로딩 순서 → 예제 에러 |
| ch06 | EDA 이론 | 양호 | 수치 오타 2건 |
| ch07 | EDA 실습 | 수치 정확도 최고 | 경미한 손질만 |
| ch08 | 확률 | 주의 | 정규 합·차 독립성 가정 누락 |
| ch09 | 표집분포·CLT | 탄탄 | 불편성 과잉 일반화 |
| ch10 | 가설검정 원리 | 양호 | p-값 재현성(정규근사 vs binom.test) |
| ch11 | 정규분포 계산 | 주의 | 독립성 가정 누락(ch08과 동일), align* |
| ch12 | 신뢰구간 | 양호 | CI 하한 오기, prop.test 설명 오류 |
| ch13 | 일표본 검정 | 양호 | 슬롯머신 p-값 5차수 오류 |
| ch14 | 일표본 신뢰구간 | **수식 전면 재점검 필요** | 수식 붕괴 15곳+, 부등호 방향 오류 |
| ch15 | 이표본 검정 | 수치 전부 정확 | z/t 혼용, 문구 오타 |
| ch16 | 이표본 신뢰구간 | **수식 전면 재점검 필요** | 수식 붕괴 12곳+, 중간 계산값 오류 |
| ch17 | 추론 R 실습 | 수치 정확 | ρ 오용, √ 누락 등식 2건 |
| ch18 | 상관관계 추론 | **실질 오류 밀도 최고** | p-값 ×2 누락 2건, aligned 깨짐 |
| ch19 | 단순 선형 회귀 | 수치 가장 탄탄 | σ̂² 오기, 가짜 summary 출력 |
| ch20 | 마무리 조망 | 가장 깨끗 | 4차원→3차원 오기 등 경미 |

수치 예제는 대부분 R 재계산과 소수점까지 일치했고(특히 ch07, ch15, ch17, ch19),
실제 문제는 **(a) 원문 변환 과정에서 붕괴된 수식(ch14·ch16 집중), (b) 소수의 확실한 수치·개념 오류, (c) 챕터 간 표기 불일치** 세 갈래로 요약된다.

---

# A. 전역(챕터 공통) 이슈 — 일괄 처리 대상

### G1. 수식 붕괴 복원 (P0) — ch14, ch16 집중
원문(PDF) 변환 시 분수 막대·근호·첨자가 소실된 패턴. 기계적 치환이 아니라 수식마다 원형 복원 필요.
- `sn` → `\frac{s}{\sqrt{n}}` / `tn-1` → `t_{n-1}` / `p(1-p)n` → `\frac{p(1-p)}{n}` / `(z^*)2...m2` → `\frac{(z^*)^2\,\cdots}{m^2}`
- 상세 위치는 챕터별 목록(ch14 §13~29, ch16 §14~26) 참조.

### G2. 유니코드 수식 문자 → LaTeX 명령 (P0/PDF 위험)
수식 내부의 `∼`(U+223C), `±`, `×`, `−`(U+2212), `˜`, 원시 그리스 문자를 LaTeX 명령으로 치환.
- `∼` → `\sim`: ch14:18, 76, 85 / ch15:104, 111, 112, 119 / ch16:121, 122, 129, 135, 157
- `±` → `\pm`, `×` → `\times`, `−` → `-`: ch16:76, 91–93, 230, 236, 264, 270, 280, 304, 307, 310, 320 / ch14:119, 277 / ch18:88–90(U+2212)
- 산문 속 원시 `μ`, `α`, `ν`, `x¯` 등 → `$\mu$` 등: ch14 다수(85, 91, 97, 131, 135, 137, 139, 211, 274, 290, 292, 294 등), ch16:117, 261, ch17:158
- 검색 팁(PowerShell/ripgrep): `rg -n "[∼±×−˜]" notes/` 로 전수 확인 가능.

### G3. 정렬 환경 통일: `align*` → `aligned` (P0/PDF 위험)
`$$ \begin{align*} … \end{align*} $$`는 MathJax는 관용하지만 LaTeX(PDF)에서는 "Erroneous nesting" 오류.
- 대상: ch11:24–29, ch11:151–155, ch14:255–262 (나머지 24곳은 이미 `aligned`)
- ch13:32–35, ch13:181–184의 `$$ H_0 … \\ H_A … $$`(환경 없는 맨 `\\`)도 `aligned`로 감싸기.

### G4. `aligned` 환경 내부 결함 (P0)
- ch18:107–112 — `begin{aligned}`/`end{aligned}`에 **역슬래시 자체가 없음** → 수식이 깨져 노출됨. `\begin{aligned}`로.
- ch17:45–50 — 두 가설 사이 `\\` 누락 → 한 줄로 붙어 렌더링.
- ch15:479–482, 561–563 — 정렬 기호 `&` 누락(다른 가설 블록과 모양 불일치).
- ch16:93 — `\ `와 `\cr` 혼용 → `\\` + 행별 `&=`로 재작성.
- ch16:216–219 — 단일 `\`로 이어붙인 두 수식 → `aligned`로 분리.

### G5. `s.e.` 표기 통일 (P2)
수식 모드 맨몸 `s.e.`는 이탤릭 변수곱처럼 렌더링. `\text{s.e.}`로 통일.
- 대상: ch09:55, 78, 84, 89 / ch11:211, 223 / ch12:134, 162, 172 / ch13:60, 63, 66, 72, 207 / ch16:193

### G6. 검정통계량 기호 z vs t (사용자 결정 필요)
$N(0,1)$과 비교하는 통계량을 책 전반에서 $t$로 표기 중(ch11, ch13 비율 검정, ch15).
표준 관례는 $z$이며, ch13 후반부터 진짜 $t_{n-1}$ 통계량이 등장해 기호 충돌.
특히 ch15:149는 $z$로 정의한 공식을 바로 아래 155행에서 $t$로 계산해 자기 불일치.
- **안 A(권장)**: 비율/알려진 σ 검정 구간(ch11 전체, ch13:54–135, ch15 비율 절)을 $z$로 전환.
- **안 B**: $t$ 표기 유지 + 최초 등장(ch11)에 "이 책은 검정통계량을 일반 기호 $t$로 쓴다" 각주.

### G7. 산문 속 H0/HA 원시 텍스트 (P0에 준함)
`H0:$\mu=4$ 대 HA:$\mu \neq 4$` 형태 → `$H_0: \mu = 4$ 대 $H_A: \mu \neq 4$`.
- 대상: ch14:280, 290, 292, 294

### G8. 용어 통일 (P2) — 권장안 기준
| 통일안(권장) | 현재 혼용 | 주요 위치 |
|---|---|---|
| 표준편차 | 표준 편차 | ch02:229, 290 / ch14 / ch16 (ch01·ch13은 붙임) |
| 중심극한정리 | 중심 극한 정리 | ch09, ch14:15, 35, 117(한 문장 내 혼용) |
| 정규분포 | 정규 분포 | ch09 ↔ ch11/ch12 |
| 귀무 가설·유의 수준 | 귀무가설·유의수준 | ch14:298, 323 (책 전반은 띄어 씀) |
| 최솟값·최댓값 | 최소값·최대값 | ch01:93 |
| 도수분포표 | 빈도표 | ch06:23 ↔ ch07:15 |
| 산포 | 퍼짐/산포도 | ch06:89, 169 ↔ ch07 |
| 다섯 수치 요약 | 다섯 수 요약 | ch06:278 ↔ ch07:91, 116 |
| 쌍봉 | 이봉형 | ch06:234 ↔ ch07:261 |
| 대응표본 | 쌍체/쌍을 이룬 | ch15 ↔ ch16:9, 257, 380 |
| 표집틀(또는 표본 추출 틀) | 두 표기 혼용 | ch03 ↔ ch04 |
| 모수 | 매개변수 | ch06 ↔ ch08:109~127, ch18:96, 132 |
| 통계적으로 유의한 | 유의미한 | ch09:37, ch10:92 ↔ ch10:171 |
| Millennial | Millenial | ch15:60–61, ch17:92–93, 111 |
| AI·ANOVA·3D | ai·anova·3d | ch20:13, 48, 56 |

### G9. 원서 참고문헌 잔재 `[숫자]` (P2)
서지 목록 없이 남은 번호 인용 → 삭제하거나 `[@key]` 인용으로 전환(결정 필요).
- 대상: ch13:173, 310 / ch14:119, 135 / ch16:85, 227, 277, 299, 317

### G10. `%` 및 신뢰수준 표기 통일 (P2)
- 수식 밖 `95\%`(이스케이프)와 `95%` 혼재 → 수식 밖은 `95%`로 통일(렌더링은 동일, 소스 일관성).
- `$(1-\alpha) \times 100\%$` / `$(1-\alpha)$ 100%` / `$(1-\alpha) \times 100$ \%` 3종 혼재 → `$(1-\alpha)\times 100\%$` 한 가지로.
- **예외(P0)**: ch14:119 `$(1−0.04)100%=96\%$` — 수식 **내부**의 미이스케이프 `%`는 TeX 주석 문자라 뒷부분이 잘려 나감 + U+2212. → `$(1-0.04)\times 100\% = 96\%$`

### G11. 표 안 절댓값 `|t|` (P2 — 실증 결과 강등)
현 설치본 pandoc/Quarto에서 수식 내 `|t|`가 표 셀을 쪼개지 않음을 실측 확인. 다만 구버전 호환과 책 내 관례(ch11:199, ch13:95는 `\vert` 사용) 통일을 위해 `\vert t \vert` 권장.
- 대상: ch15:168, 395, 536 / ch18:90

### G12. 코드 펜스 ` ``` {r}`(공백) (P2 — 실증 결과 강등)
Quarto 실측 결과 공백이 있어도 정상 실행됨. 버그 아님. 다만 ` ```{r} `로 표기 통일 권장.
- 대상: ch14:306, 327 / ch20:84, 95

### G13. 기타 전역
- 연도 표기: ch08:33 "2025년" → "2026년"(같은 파일 141행 및 ch05~ch07과 통일). (P2)
- R 함수명 표기: 백틱 코드로 통일 — ch14:131 `$t.test$`(수식 모드), ch17:97 `$x=c(467,407)$` 등. (P2)

---

# B. 챕터별 상세 수정 목록

## ch01 — 통계학 소개
1. **(P1) ch01:19** 머니볼 시즌: "2001년" → **2002년**(103승, 20연승). 빌리 빈 취임은 1997년이므로 "2001년 도입"도 부정확.
2. (P1) ch01:9 "디즈레일리의 농담" → 귀속 불확실. "디즈레일리가 남겼다고 전해지는(마크 트웨인이 인용해 유명해진)"으로 완충.
3. (P1) ch01:51 p-값 예고 문구: "우리가 가진 데이터가 나올 확률" → "그 이상으로 극단적인 데이터가 나올 확률"로 오개념 예방.
4. (P2) ch01:93 "최소값/최대값" → "최솟값/최댓값" (G8).
5. (P2) ch01:7 "화학을 논했고" → "자연학(물리학)을 논했고" 권장.
6. (P2) ch01:5 기사 날짜(8/5)와 URL 날짜(8/6) 불일치 → "2009년 8월 6일자(온라인판 8월 5일)".

## ch02 — R 입문
1. (P1) ch02:17–19 RStudio 설치 UI 서술이 구형 Rgui 방식 → "Tools > Install Packages... 대화상자에 패키지명 입력" 흐름으로 교체.
2. (P1) ch02:137 팩터의 이점을 "처리 속도" 위주로 서술 → 수준(level) 관리·모델링 더미 처리 등 통계적 이점으로 교체.
3. (P1) ch02:264, 268, 316–318 "Singing in the Rain" → **"Singin' in the Rain"**; The Godfather 상영 시간 177 → 175분 확인 후 통일.
4. (P2) ch02:60 변수명 규칙 보완: 마침표(`.`) 허용, 숫자/언더바로 시작 불가 명시.
5. (P2) ch02:229, 290 "표준 편차" → "표준편차" (G8).
6. (P2) ch02:294 연습 문제의 `setwd()` 요구가 본문 권고(201, 302행)와 충돌 → 문제 재구성.
7. (P2) ch02:300 "2026년 4월 24일 릴리스된 R 4.6.0" — 실제 일자 확인 또는 일자 생략.

## ch03 — 데이터 수집 (내용 수정 시급)
1. **(P1) ch03:48** 유도 질문(leading question)을 표본 추출 편향의 예로 제시 → **응답 편향**(60행)의 예로 이동. 표본 추출 편향 예시는 편의 표집·자원 표집·미포함(undercoverage)으로 교체.
2. **(P1) ch03:52–54** 1948년 선거: "전화 조사, 부유층=듀이 지지" 서술은 사실과 다름(당시는 대면 **할당 표집**; 전화기 보급 편향 이야기는 **1936년 Literary Digest** 사례). → 할당 표집+조사 조기 종료로 교체하고, 전화 일화는 1936년 사례로 분리 소개 권장.
3. (P1) ch03:62 2016년 선거: "압승 예측" 과장 + 샤이 트럼프 단정 → "박빙 우세" + AAPOR 검증(학력 가중 미보정 등) 병기로 균형 서술.
4. (P2) ch03:64 "가중치 부여 = AI 기법" → 고전적 조사통계 기법(사후층화)으로 정정.
5. (P2) ch03:76 "대조해 보겠습니다" → "비교합니다"(정의문에 청유형 오용).
6. (P2) ch03:140 "P-hacking" → "p-해킹(p-hacking)", `$p$-value < 0.05` → `$p < 0.05$`.

## ch04 — R 데이터 추출
1. **(P0) ch04:171** `$2^{19937-1}$` → **`$2^{19937}-1$`** (메르센 트위스터 주기; 현재는 2^19936을 의미). "약"도 삭제(정확값).
2. **(P1) ch04:173** seed를 "척도라는 의미" → **"씨앗이라는 의미"** (오역).
3. (P1) ch04:33 대통령 취임 나이 벡터: 8번째 이후 실제와 불일치(38, 67은 존재하지 않는 값) → 실제 값으로 교체하거나 "가상의 예제" 명시.
4. (P1) ch04:5 서론 `sample()` vs 본문 `sample.int()`(183행~) 불일치 → 서론 수정 또는 관계 설명 추가.
5. (P2) ch04:115–117 모의 출력 형식이 실제 R 출력(헤더+행 이름)과 다름 → 127–132행 형식으로 통일.
6. (P2) ch04:158–163 출력 블록 ` ```r ` → ` ```text `로 통일.
7. (P2) ch04:247 "$400,000" → `\$400,000` 또는 "40만 달러"(수식 오인 예방).
8. (P2) ch04:150 `` `$ ` `` 코드 스팬 내 공백 제거.
9. (P2) ch04:68 실수 `==` 비교 각주(부동소수점 함정) 추가 권장.

## ch05 — 데이터 불러오기
1. **(P1) ch05:78–105** `library(tidyverse)` → `library(MASS)` 순서로 로드 후 `select()` 사용 → **`MASS::select`가 `dplyr::select`를 마스킹해 예제가 실제로 에러**(재현 확인). MASS를 먼저 로드하거나 `dplyr::select()` 명시. ch07:8–10(올바른 순서)과 통일.
2. (P1) ch05:40–52 모의 콘솔 출력의 `masks MASS::select` 줄은 현재 시나리오에서 재현 불가 → 위 수정과 함께 정리.
3. (P1) ch05:17 `nlschools`: "중학교 2학년" → "8학년(초등 최종 학년, 만 11세가량) 학생 2,287명" (MASS 문서 기준).
4. (P2) ch05:72 "라이브러" → "라이브러리" 오타.

## ch06 — EDA 이론
1. **(P1) ch06:31** `577/1488 ≈ 0.378` → **0.388** (재계산 확인).
2. **(P1) ch06:121** 정렬 목록 오기: "349, …, 500, …" → **"389, 432, 433, 433, 440, 440, 458, 484, 497, 508, 526, 531, 548, 550"** (평균 476.36·중앙값 471은 원자료 기준 정확).
3. (P2) ch06:33 분할표 크기 "$m \times p$" → "$r \times c$" ($p$는 비율 기호와 충돌).
4. (P2) ch06:163–165 `$\bar{DDT}$` 등 → `$\overline{\text{DDT}}$`, `$\widetilde{\text{DDT}}$`, `$\text{DDT}_{(3)}$`.
5. (P2) ch06:247 수식 내 `IQR` → `\mathrm{IQR}`.
6. (P2) ch06:183 표준편차 도입 이유: "값 팽창" → "단위가 제곱으로 바뀌는 문제" 로 정정.
7. (P2) ch06:279–284 나란한 상자그림 설명에 `boxplot(y ~ group)` 문법 코드 추가.
8. (P2) ch06:25, 35, 74 "퓨 리서치 그룹" → "퓨 리서치 센터(Pew Research Center)".

## ch07 — EDA 실습
1. (P1) ch07:253 "앞서 언급된 `geyser`" → 최초 등장이므로 소개 문구로 교체.
2. (P1) ch07:97–101 `range()`는 (최솟값, 최댓값) 벡터 반환 — "범위를 곧바로 반환" 서술 정정(`diff(range(x))` 안내).
3. (P2) ch07:91 "최댓값에서 최솟값을 뺀 절댓값" → "뺀 값".
4. (P2) ch07:122–126 "콘솔에 다음과 같이" + 5개 값 표 → Mean 포함 6개 값이 실제 출력. 문구 조정.
5. (P2) 용어 통일(G8): 빈도표/산포/다섯 수치 요약/이봉형.

## ch08 — 확률
1. **(P1) ch08:141–155** 정규분포 합·차 성질에 **독립 가정 누락** → "서로 독립인 $X$, $Y$에 대해" 명시(일반적으로 $\mathrm{Var}(X\pm Y)=\sigma_X^2+\sigma_Y^2\pm 2\,\mathrm{Cov}(X,Y)$). ch11 §1과 동일 사안 — 함께 수정.
2. (P2) ch08:33 "2025년" → "2026년" (G13).
3. (P2) ch08:25–27 대수의 법칙: `$\bar{x}_n$, $\hat{p}_n$` 첨자 보강 + 두 식 사이 쉼표 + "확률적으로 수렴" 문구 권장.
4. (P2) ch08:47–49 수식 내 영어 설명(`n of outcomes in A`) → 한국어 `\text{}`로 (ch06:19 방식과 통일).
5. (P2) ch08:109 등 "매개변수" → "모수" (G8).
6. (P2) ch08:109 확률 분포 정의가 이산형 전용 → "값(또는 값의 구간)"으로 완화.
7. (P2) ch08:80–91 "### 연습 문제" 제목 아래 문제 없음 → 제목 변경 또는 문제 추가.

## ch09 — 표집분포·CLT
1. **(P1) ch09:69** "가능한 모든 표본 통계량의 평균 = 모수" 일반화 → **불편 추정량에 한정**해 서술(예: $s$는 편향).
2. (P2) ch09:55–59 유한 모집단($N=5$) 예시에서 `$\lim_{n\to\infty}$` → "$n=N$이면 s.e.=0" 또는 무한 모집단 가정 명시.
3. (P2) ch09:75–85 `$\bar{x} \sim N(\mu, \text{s.e.}^2)$ as $n \to \infty$` → "as n→∞" 삭제하고 "n이 충분히 크면 근사적으로" 서술(퇴화분포 자기모순 회피).
4. (P2) ch09:55, 78, 84, 89 맨몸 `s.e.` → `\text{s.e.}` (G5).
5. (P2) "중심 극한 정리/정규 분포" 띄어쓰기 (G8).

## ch10 — 가설검정 원리
1. (P1) ch10:151 주사위 p-값 "0.01": 본문 실습 도구 `binom.test(25,100,1/6,"greater")`로는 0.0217 → 계산 방법(정규근사) 병기 또는 값 교체. (결정 필요 — D4)
2. (P1) ch10:161 동일 사안: 0.5485는 정규근사값, binom.test는 0.6173.
3. (P1) ch10:127 "1종 오류가 일반적으로 훨씬 치명적" 단정 → 검정 체계의 설계(1종 오류율 통제)로 서술 완화.
4. (P2) ch10:12 "조류학자" → "조류(藻類)학자"(새로 오독 방지), "실험역" → "농업시험장", 브리스톨도 로담스테드 소속.
5. (P2) ch10:44 `$Pr(H)$` → `$P(H)$` (132행 `\text{Pr}`, 타 챕터 `P()`와 3종 혼재).

## ch11 — 정규분포 계산·검정통계량
1. **(P1) ch11:21–29** 정규 합·차 **독립성 가정 누락** (ch08 §1과 동일) → "서로 독립인" 명시.
2. **(P0/PDF) ch11:24–29, 151–155** `align*` → `aligned` (G3).
3. (P2) ch11:171~199 $t$ 기호 → z/t 결정(G6) 대상.
4. (P2) ch11:211, 223 맨몸 `s.e.` → `\text{s.e.}` (G5).
5. (P2) ch11:47–48 `pnorm()` 서술 일반화("$N(0,1)$ 한정" 표현, "요구합니다" → "받습니다").

## ch12 — 신뢰구간
1. **(P1) ch12:137, 144** CI 하한 `0.7585` → **0.7587**(표시 수치 기준) 또는 R 코드 출력과 통일해 **(0.7588, 0.8870)**.
2. **(P1) ch12:227–231** "`correct=FALSE`면 수동 Wald 구간과 매우 유사" → **`prop.test`는 Wilson 점수 구간**이라 (0.7502, 0.8779)로 눈에 띄게 다름. 설명 교체.
3. (P2) ch12:86 "-1을 곱해 … 좌우 대칭이므로" → 부등호 반전 후 재배열로 근거 정정.
4. (P2) ch12:134, 162, 172 맨몸 `s.e.` → `\text{s.e.}` (G5).
5. (P2) 수식 밖 `\%` 이스케이프 혼재 정리 (G10).

## ch13 — 일표본 검정
1. **(P1) ch13:268, 274** 슬롯머신 p-값 `1.834×10⁻²⁴⁷` → **`1.377×10⁻²⁴²`** (`pt(-90.7084, 344)` 재계산; 5차수 차이).
2. (P1) ch13:125–135 경계선 예제 반올림 전파: p̂=0.549로 t=−1.644/p=0.0501 vs 원값 826/1503으로 **t=−1.600/p=0.0548** → 원값 계산으로 교체 또는 각주.
3. (P1) ch13:97 성공-실패 조건: 검정에서는 `$n\hat{p}$` 아닌 **`$np_0$`** 기준(69–73행 유도와 정합; CI는 p̂ 기준 — ch14:35와 대비 명시).
4. (P1) ch13:204–207 `s.e. = σ/√n`을 "중심극한정리에 의해" → 분산 성질에 의한 결과로 정정(CLT는 정규 근사 담당).
5. (P1) ch13:232, 244 "완벽한 N(0,1)"·"수학적으로 증명" → "모집단이 정규이거나 n이 크면" 전제 삽입.
6. (P1) ch13:89 p-값 요약에 "귀무 가설이 참일 때" 조건 누락 → 보강.
7. (P1) ch13:324 "두 집단 간 차이" → 일표본 장이므로 "통계량과 귀무 기준값 간 차이".
8. **(P0/PDF) ch13:32–35, 181–184** `$$` 내 맨 `\\` → `aligned` (G3).
9. (P2) ch13:54–135 비율 검정 $t$ 기호 → z/t 결정(G6) 대상.
10. (P2) ch13:60~72, 207 맨몸 `s.e.` → `\text{s.e.}` (G5).
11. (P2) ch13:26–43(`####`) vs 49~109(`###`) 헤딩 레벨 통일.
12. (P2) ch13:173, 310 참고문헌 잔재 (G9).

## ch14 — 일표본 신뢰구간 (수식 전면 재점검)
**P0 — 붕괴 수식 복원 (원형 명시):**
1. ch14:18 `\hat{p}∼N(p,p(1-p)n)` → `\hat{p} \sim N\left(p,\ \frac{p(1-p)}{n}\right)`
2. ch14:76 `\bar{x}-\mu_{s}/n∼t_{n-1}` → `\frac{\bar{x}-\mu}{s/\sqrt{n}} \sim t_{n-1}`
3. ch14:82 `P(-t^*<tn-1<t^*)` → `P(-t^* < t_{n-1} < t^*)`
4. ch14:85 산문에 원시 수식 노출(`x¯−μs/n∼$tn-1$` + 훼손 인라인 2개) → 문장 전체 재작성(위 2번 형태 사용).
5. ch14:88 `P(-t^*<\bar{x}-\mu_{s}/n<t^*)` → `P\left(-t^* < \frac{\bar{x}-\mu}{s/\sqrt{n}} < t^*\right)`
6. ch14:93–95 4단계 유도가 한 줄로 연결 + `sn` 훼손 → `aligned` 4행으로 재작성 (각 행 `&=`).
7. ch14:100, 267 `(\bar{x}-t^*sn, \bar{x}+t^*sn)` → `\left(\bar{x}-t^*\frac{s}{\sqrt{n}},\ \bar{x}+t^*\frac{s}{\sqrt{n}}\right)`
8. **ch14:119** 수식 내 미이스케이프 `%` — 뒷부분 잘림(G10 예외) → `$(1-0.04)\times 100\% = 96\%$`
9. ch14:156 `\hat{p}±{z^*}\hat{p}(1-\hat{p})n` → `\hat{p} \pm z^*\sqrt{\frac{\hat{p}(1-\hat{p})}{n}}`
10. ch14:162 `n=({z^*})2\hat{p}(1-\hat{p})m2` → `n = \frac{(z^*)^2\,\hat{p}(1-\hat{p})}{m^2}`
11. ch14:172 동일 + `p˜` 깨짐 → `n \ge \frac{(z^*)^2\,\tilde{p}(1-\tilde{p})}{m^2}`
12. **ch14:187 부등호 방향 반대**: `P(N(0,1)>z^*)=0.9975` → **`P(N(0,1)<z^*)=0.9975`** (이대로면 z*≈−2.807).
13. ch14:213 `\text{s.e.}(\bar{x})=sn` → `=\frac{s}{\sqrt{n}}`
14. ch14:246 `|\bar{x}-\mu_0s/n|<t^*` → `\left|\frac{\bar{x}-\mu_0}{s/\sqrt{n}}\right| < t^*`
15. **ch14:255–262** `align*` 중첩 + `sn` 훼손 + **마지막 줄 여분의 `\bar{x}-`로 수학적으로 틀린 결론** → `aligned`로 4행 재작성(마지막 행 `\bar{x}-t^*\frac{s}{\sqrt{n}} < \mu_0 < \bar{x}+t^*\frac{s}{\sqrt{n}}`).
16. ch14:277 `3.9865±2.093×0.07320` — `0.073/√20`이 한 숫자처럼 붙음 → `3.9865 \pm 2.093 \times \frac{0.073}{\sqrt{20}} = (3.952,\ 4.021)`

**P1 — 내용:**
17. ch14:288 통밀가루 표본 "28개" → **24개** (`MASS::chem` n=24; 자유도·답 달라짐). "4.1mg" 단위도 ppm으로 보완.
18. ch14:294 99% CI ↔ "α=0.005" 쌍대성 불일치 → **α=0.01** (문제 10·11 패턴상 오타 판단).
19. ch14:135 오존 "ppm" → **ppb** (`airquality` 단위).
20. ch14:231 존재하지 않는 그림 참조 + $P(t_{n-1}\ge|t|)$를 p-값이라 서술(실제는 절반) → 문장 삭제 또는 재작성.
21. ch14:343 중복/자리 이탈 문단("이 출력에서…") → 삭제 또는 323행과 통합.
22. ch14:61 크랩스 문제(n=20, 성공 8)가 35행 조건 위배 → "가정 충족 여부도 확인" 문구 추가.

**P2 — 표기:**
23. ch14:280, 290, 292, 294 H0/HA 원시 텍스트 (G7).
24. ch14 전반 원시 μ/α/ν (G2). 특히 211행 제목 "### μ에 대한…" ↔ 65행 `## $\mu$에 대한…` 불일치.
25. ch14:9~270 신뢰수준 표기 3종 혼재 (G10).
26. ch14:97 `Lower/Upper` 맨몸 → `\text{lower}/\text{upper}` (70행과 통일).
27. ch14:46 `\sqrt{\frac{325/362(1-325/362)}{362}}` → p̂ 정의 후 `\left(\ \right)` 사용.
28. ch14:112 `qt(p = p_value, …)` 자리표시자명 → `prob` 등(p-값 혼동 방지).
29. ch14:131 `$t.test$` → 백틱 코드 (G13).
30. ch14:306, 327 ` ``` {r}` → ` ```{r}` (G12 — 실행은 됨).
31. ch14:307–309 "임의의 데이터 생성" 주석 + 무의미한 `set.seed` → 정리.
32. ch14:119, 135 참고문헌 잔재 (G9).

## ch15 — 이표본 검정
1. (P1) ch15:201 `P(N(0,1)>0.889)` → **0.8829** (198행·R 코드와 일치).
2. (P1) ch15:182 "선거 예시를 요약하자면" → **"우주 탐사 예시"** (해당 절 예시명 오기).
3. (P1) ch15:182 "차이가 없다고 판단" → "차이가 있다는 증거를 찾지 못함"(귀무가설 채택 어감 제거).
4. (P1) ch15:403 p-값 이중 표기 "0.00266(또는 0.00252)" → **0.00252로 단일화** (399행 `pt()` 결과).
5. (P1) ch15:567 "보수적인 α=0.1" → 용어 정반대. "관대한(느슨한)" 또는 수식어 삭제 (418행 "보수적인 α=0.05"도 점검).
6. (P0/PDF) ch15:104, 111, 112, 119 유니코드 `∼` → `\sim` (G2; 같은 파일 305행 등은 올바름).
7. (P2) ch15:149(z 정의) vs 155, 164–172(t 사용) 기호 자기 불일치 → 비율 절은 $z$로 통일 권장 (G6).
8. (P2) ch15:479–482, 561–563 `aligned` 내 `&` 누락 (G4).
9. (P2) ch15:168, 395, 536 표 안 `|t|` → `\vert t \vert` (G11).
10. (P2) ch15:60–61 `p_{\texttt{Millenial}}` → `p_{\text{Millennial}}` (철자+서체, G8).
11. (P2) ch15:29 `p _2` 공백 오타 → `p_2`.
12. (P2) ch15:73 `407/558≈0.7293` → 0.7294(반올림). 155·198행 "반올림 전 값으로 계산" 각주 권장.
13. (P2) ch15:101 "standard Error" 대소문자, 160행 "단일 표본 p 검정" → "비율 검정", 570행 수식 끝 마침표 위치.

## ch16 — 이표본 신뢰구간 (수식 전면 재점검)
**P0 — 붕괴 수식 복원:**
1. ch16:48 SE가 `p_1(1-p_1)n_1+p_2(1-p_2)n_2`로 표기(나눗셈·근호 소실) → `\sqrt{\frac{p_1(1-p_1)}{n_1}+\frac{p_2(1-p_2)}{n_2}}`
2. ch16:54, 58 표준화 단계 누락된 채 "표준 정규를 따른다" 단정 → 54행을 `\frac{\hat{p}_1-\hat{p}_2-(p_1-p_2)}{\sqrt{\frac{\hat{p}_1(1-\hat{p}_1)}{n_1}+\frac{\hat{p}_2(1-\hat{p}_2)}{n_2}}}\sim N(0,1)`로 교체.
3. ch16:61, 68 분수 막대 소실(√가 곱으로 읽힘) → `\frac{\hat{p}_1-\hat{p}_2-(p_1-p_2)}{\sqrt{\cdots}}` 복원.
4. **ch16:93 중간 계산값 오류**: SE `0.0271`/오차한계 `0.0629` → **0.0235 / 0.0547** (최종 구간 (0.1249, 0.2343)은 정확 — 중간값만 자기모순). 같은 줄 `\ `/`\cr` 혼용도 `\\`+`&=`로 재작성 (G4).
5. ch16:121–138 (5곳) `\sigma_1^2n1` 형태(첨자·나눗셈 소실) → `\frac{\sigma_1^2}{n_1}+\frac{\sigma_2^2}{n_2}` 복원.
6. ch16:145 합동분산 분자 `(n_2-1)s^2` → **`(n_2-1)s_2^2`** (첨자 누락; ch15:334와 통일).
7. ch16:211 `(\mu_1-\mu_2) / \sqrt{…}` 슬래시가 마지막 항에만 걸림 → `\frac{}{}` 형태로(217행 정상 표기와 통일).
8. ch16:216–219 단일 `\`로 이어붙인 수식 → `aligned` 재작성 (G4).
9. ch16:230 `(15-1)×758.667+…+15-2` 분수 소실 → `\frac{(15-1)(758.667)+(15-1)(1688.352)}{15+15-2}=1223.51`
10. ch16:236 `1.7011223.51(115+115)` — 숫자 붙음·√ 소실·`1/15` 붕괴 → `68.333-94.067 \pm 1.701\sqrt{1223.51\left(\frac{1}{15}+\frac{1}{15}\right)}=(-47.460,\ -4.008)`
11. **ch16:280, 283 구간 순서 뒤집힘**: `(-0.027, -0.233)` → **`(-0.233, -0.027)`** (두 곳).
12. ch16:310 `\sqrt{2,680.182(1/14+1/12})` 괄호 짝 어긋남 → `\sqrt{2680.182\left(\frac{1}{14}+\frac{1}{12}\right)}` (수식 내 천 단위 쉼표도 제거).
13. ch16 전반 유니코드 `±`, `×`, `∼`, `−` (G2 목록 참조).

**P1 — 내용/링크:**
14. ch16:99 `[제17장](ch17.md)` → **`(ch17.qmd)`** (깨진 내부 링크).

**P2 — 표기:**
15. ch16:111 "(하한, 상한$)$" → 불필요한 수식 모드 괄호 제거.
16. ch16:114 `Lower/Upper` → `\text{Lower}/\text{Upper}` (20행과 통일).
17. ch16:193 `s.e.` → `\text{s.e.}` (G5).
18. ch16:9, 257, 380 쌍체/쌍을 이룬/대응 표본 → "대응표본" 통일 (G8).
19. ch16:85~317 참고문헌 잔재 5곳 (G9).
20. ch16:117, 261, 267 산문 속 원시 μ/s/n → `$…$` (G2).
21. ch16:223 "심혈를" → "심혈을"; 277행 "화분석" 용어 확인; 304행 `2,384.99` → `2384.99` 또는 `2{,}384.99`.
22. ch16:109 제목의 U+2212 vs 293행 ASCII `-` 통일.

## ch17 — 추론 R 실습
1. **(P0) ch17:39–48, 86, 92–93** 모집단 비율 **`\rho` → `p`** (책 전체 관례 및 ch18의 ρ=상관계수와 충돌; 39행은 한 수식에 ρ·p 혼재; 23·111·115–121행은 이미 p).
2. **(P0) ch17:45–50** `aligned` 내 `\\`(및 `&`) 누락 → 두 가설이 한 줄로 붙음 (G4).
3. **(P0) ch17:66** `$+0.0929=0.3048$` 거짓 등식 → `$+\sqrt{0.0929}=0.3048$`; `$X-squared$` → 백틱 코드.
4. **(P0) ch17:108** `$-1.2707 = -1.127$` → `$-\sqrt{1.2707} = -1.127$`.
5. **(P0) ch17:139–141** `\mu 0` (3곳) → `\mu_0`.
6. (P0) ch17:92–93 `\rho_{\text{Gen}}X` — X가 첨자 밖 → `p_{\text{GenX}}`; "Millenial" 철자 (G8).
7. (P1) ch17:76 비율 단측 CI "무한대(또는 0)로 열려" → **0 또는 1에서 잘림**; "잘못된 구간" 표현도 "단측 구간"으로 정정 (199행 t.test의 "Inf"는 올바름).
8. (P1) ch17:183 분산비 `s_1²/s_2² = 27.855/19.087 = 1.459` → 집단 1=high이므로 **19.087/27.855 = 0.685** (ch15:363과 모순 해소).
9. (P1) ch17:143 "적거나 같은지" → "더 적은지" (`alternative="less"`의 대립가설).
10. (P1) ch17:13 "즉, 교집합이…존재합니까?" → "아니면 …존재합니까?" (논리 반전).
11. (P2) ch17:68, 156 백틱 안 `$p$-value` → `p-value` 또는 "$p$-값".
12. (P2) ch17:97 `$x=c(467,407)$` → 백틱 코드 (G13).
13. (P2) ch17:158 산문 원시 μ → `$\mu$` (G2).

## ch18 — 상관관계 추론
1. **(P0) ch18:107–112** `begin{aligned}` **역슬래시 누락 → 수식 깨져 노출** → `\begin{aligned}…\end{aligned}`.
2. **(P1) ch18:92, 94** cars p-값: ×2 선언 후 미적용 — `7.45×10⁻¹³` → **`1.49×10⁻¹²`** (cor.test와 일치).
3. **(P1) ch18:120** Galton p-값: 동일 문제 — `8.46×10⁻⁵⁰` → **`1.69×10⁻⁴⁹`**.
4. (P1) ch18:156 피셔 z-변환 CI "정규성 불필요" 단정 → 이변량 정규성 하 유도된 근사임을 반영해 완화.
5. (P1) ch18:82 "정규성 위반 → 검정력 약화" 단정 → "p-값·1종 오류율을 액면대로 신뢰하기 어려움"으로 수정.
6. (P2) ch18:80 정규성 가정: 각 변수 주변 정규 → "(엄밀히는 이변량 정규)" 각주.
7. (P2) ch18:88–90 U+2212 3곳 → ASCII `-`; 90행 `|t|` → `\vert t \vert` (G11).
8. (P2) ch18:86 표 머리글 `H_A` → `$H_A$`.
9. (P2) ch18:9 vs 11 r=0.8069 강도 서술 상충("중간" vs "강한") → 통일.
10. (P2) ch18:67, 73, 117 슬래시 분수 → `\frac` (ch19:360과 통일).
11. (P2) ch18:96, 132 "매개변수" → "모수" (G8).

## ch19 — 단순 선형 회귀
1. **(P1) ch19:353–355** $\hat{\beta}_1$ 표본분포 분산의 `\hat{\sigma}^2` → **`\sigma^2`** (추정치 대입 시 t-분포 전개(373행)와 논리 충돌; 357행 s.e. 추정치는 그대로).
2. **(P1) ch19:170–173, 403–406, 469–472** 가공된 `summary(slr)` 출력: 유의성 별표 소실(`Signif. codes: 0 '' 0.001 ''…`), 세 사본끼리도 불일치, 413행 별표 설명과 모순 → 실제 출력 원문으로 교체하거나 실행되는 ` ```{r} summary(slr)` 청크로 전환.
3. (P0) ch19:177, 410, 476 출력 블록 내 `$p$-value:` → `p-value:` (달러 기호 그대로 노출).
4. (P1) ch19:429–431 존재하지 않는 "왼쪽/오른쪽 산점도" 참조(SSR 2421.9/12050.5) → 그림 생성 코드 삽입 또는 자족적 서술로 재작성.
5. (P1) ch19:289 "기댓값 0 가정은 수학 법칙으로 자동 충족" → 잔차 평균=0은 OLS 대수적 성질일 뿐(가정 점검 불가) 서술 교체.
6. (P1) ch19:9 "인과적 역학" + "상관관계(R²에 기반한 강도)" → 인과 표현 완화(555행 자기 경고와 정합), 강도는 |r|로.
7. (P2) ch19:84, 158 반올림 입력의 등호 → `≈` 또는 각주 (10.7296, 34.9755는 비반올림 원값).
8. (P2) ch19:421–424 SSR 명칭이 외부 관례(SSR=회귀제곱합)와 충돌 → RSS 병기 각주.
9. (P2) ch19:204–210 `fitted()` "리스트" → "벡터".

## ch20 — 마무리 조망
1. (P1) ch20:60 "3차원의 복잡한 궤적을 2차원에 압축" → **4차원** (48행: 속도·회전수·수직·수평 4개 변수; "3"은 구종 수와 혼동).
2. (P2) ch20:15 "말밤" → "잠두(horsebean)" 또는 "누에콩".
3. (P2) ch20:54 `$r^2$` → `$R^2$` (ch19·R 출력과 통일).
4. (P2) ch20:13, 48, 56 ai/3d/anova → AI/3D/ANOVA (G8).
5. (P2) ch20:2, 9 "몇가지" → "몇 가지"; 104행 "단 번의" → "단 한 번의".
6. (P2) ch20:84, 95 ` ``` {r}` 공백 → 통일 (G12 — 실행은 됨).

---

# C. 권장 작업 순서

1. **ch16 → ch14 수식 전면 복원** (붕괴 최다·수학적 오류 포함, 붕괴 패턴이 유사해 같이 작업하면 효율적. 두 파일 모두 git 수정 중이므로 먼저 현 변경분 커밋/정리 권장)
2. **ch18** (begin{aligned} 깨짐 + p-값 ×2 오류 2건 — 작지만 확실한 오류)
3. **나머지 P0** — ch17(ρ, √, μ₀, aligned), ch04:171, ch05 로딩 순서, ch11/ch13 `align*`·`\\`, ch15 `∼`, ch19 `$p$-value`
4. **P1 내용 수정** — ch03(역사·분류) → ch08·ch11(독립성) → ch12(CI 하한, prop.test) → ch13(p-값, 조건) → ch14·ch15·ch16 잔여 → ch01, ch09, ch10, ch19, ch20
5. **P2 전역 치환** — 유니코드 기호(G2), `\text{s.e.}`(G5), 용어 표(G8), %(G10), `\vert`(G11), 펜스(G12)
   - `rg -n "[∼±×−˜]" notes/`, `rg -n '[^\\]s\.e\.' notes/` 등으로 전수 확인 후 치환
6. **검증** — `quarto render` 전체(HTML) + 수치 있는 챕터 스팟 재계산 + (PDF 재활성화 예정이라면 PDF 시험 빌드로 G2·G3 재확인)

# D. 사용자 결정 필요 사항

1. **z vs t 기호(G6)** — 표준 관례($z$)로 전환할지, 책 고유 관례($t$ 통일 + 각주)로 갈지. 전환 시 ch11·ch13·ch15·ch17의 표와 본문 다수 수정.
2. **참고문헌 잔재(G9)** — 삭제 vs `references.bib` 기반 `[@key]` 인용 전환.
3. **용어 통일(G8)** — 표의 권장안 확정(특히 표준편차/도수분포표/대응표본/모수).
4. **ch10 p-값 예제** — 정규근사 값 유지+방법 병기 vs `binom.test` 값으로 교체(본문 실습 도구와 재현성 일치).
5. **ch02:264 Godfather 상영 시간**(177 vs 175), **ch02:300 R 4.6.0 릴리스 일자** — 사실 확인 후 확정.
