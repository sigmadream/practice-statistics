{
  "hash": "3d01423c5cd519c58c037a776d50b81a",
  "result": {
    "engine": "knitr",
    "markdown": "---\ntitle: \"시각화: 계층\"\n---\n\n\n::: {.cell}\n\n:::\n\n\n> 그래픽의 레이어 문법(layered grammar of graphics)을 배우며 기초를 확장합니다. 먼저 시각 속성(aesthetic mappings), 도형(geometric objects), 분할(facets)을 더 깊이 알아보겠습니다.\n\n막대 그래프의 막대 높이나 상자 그림의 중앙값처럼 그래프할 새 값을 계산하는 과정은 필수적입니다. 그래프에 도형(geoms)이 표시되는 방식을 수정하는 위치 조정(position adjustments)도 필요합니다. 이 장에서는 이런 시각화 정보를 다루는 방법을 소개합니다.\n\n\n::: {.cell}\n\n```{.r .cell-code}\nlibrary(tidyverse)\n```\n:::\n\n\n## 시각 속성\n\n> \"그림의 큰 가치는 전혀 기대하지 않았던 것을 보게 만들 때 나타난다.\" - 존 튜키(John Tukey)\n\nggplot2 패키지와 함께 제공되는 `mpg` 데이터 프레임에 38개 자동차 모델의 관측치 234개가 포함되어 있다는 점을 기억하세요.\n\n\n::: {.cell}\n\n```{.r .cell-code}\nmpg\n#> # A tibble: 234 × 11\n#>   manufacturer model displ  year   cyl trans      drv     cty   hwy fl   \n#>   <chr>        <chr> <dbl> <int> <int> <chr>      <chr> <int> <int> <chr>\n#> 1 audi         a4      1.8  1999     4 auto(l5)   f        18    29 p    \n#> 2 audi         a4      1.8  1999     4 manual(m5) f        21    29 p    \n#> 3 audi         a4      2    2008     4 manual(m6) f        20    31 p    \n#> 4 audi         a4      2    2008     4 auto(av)   f        21    30 p    \n#> 5 audi         a4      2.8  1999     6 auto(l5)   f        16    26 p    \n#> 6 audi         a4      2.8  1999     6 manual(m5) f        18    26 p    \n#> # ℹ 228 more rows\n#> # ℹ 1 more variable: class <chr>\n```\n:::\n\n\n`mpg`의 변수 중에는 다음이 있습니다.\n\n1. `displ`: 자동차의 엔진 크기(리터), 숫자형(numerical) 변수입니다.\n\n2. `hwy`: 고속도로에서의 자동차 연료 효율(mpg, 갤런당 마일), 연료 효율이 낮은 자동차는 같은 거리를 주행할 때 연료 효율이 높은 자동차보다 더 많은 연료를 소비합니다. 숫자형 변수입니다.\n\n3. `class`: 자동차의 유형, 범주형(categorical) 변수입니다.\n\n여러 `class`의 자동차에서 `displ`과 `hwy`의 관계를 시각화하며 시작하겠습니다.\n\n숫자형 변수는 `x`와 `y` 시각 속성에, 범주형 변수는 `color`나 `shape` 같은 시각 속성에 매핑(Aesthetic mappings)한 산점도를 사용합니다.\n\n\n::: {.cell layout-ncol=\"2\"}\n\n```{.r .cell-code}\n# Left\nggplot(mpg, aes(x = displ, y = hwy, color = class)) +\n  geom_point()\n\n# Right\nggplot(mpg, aes(x = displ, y = hwy, shape = class)) +\n  geom_point()\n#> Warning: The shape palette can deal with a maximum of 6 discrete values because more\n#> than 6 becomes difficult to discriminate\n#> ℹ you have requested 7 values. Consider specifying shapes manually if you\n#>   need that many of them.\n#> Warning: Removed 62 rows containing missing values or values outside the scale range\n#> (`geom_point()`).\n```\n\n::: {.cell-output-display}\n![](ch05_files/figure-html/unnamed-chunk-3-1.png){fig-alt='나란히 있는 두 개의 산점도로, 둘 다 자동차의 고속도로 연료\n효율 대 엔진 크기를 시각화하고 음의\n연관성을 보여줍니다. 왼쪽 그래프에서 class는 색상\n시각 속성에 매핑되어 각 class에 대해 다른 색상을\n나타냅니다. 오른쪽 그래프에서 class는 모양 시각 속성에 매핑되어\nsuv를 제외한 각 class에 대해 다른 그래프 문자 모양을\n나타냅니다. 각 그래프에는 색상 또는 모양과\nclass 변수의 수준 간의 매핑을 보여주는 범례가 함께\n제공됩니다.' width=384}\n:::\n\n::: {.cell-output-display}\n![](ch05_files/figure-html/unnamed-chunk-3-2.png){fig-alt='나란히 있는 두 개의 산점도로, 둘 다 자동차의 고속도로 연료\n효율 대 엔진 크기를 시각화하고 음의\n연관성을 보여줍니다. 왼쪽 그래프에서 class는 색상\n시각 속성에 매핑되어 각 class에 대해 다른 색상을\n나타냅니다. 오른쪽 그래프에서 class는 모양 시각 속성에 매핑되어\nsuv를 제외한 각 class에 대해 다른 그래프 문자 모양을\n나타냅니다. 각 그래프에는 색상 또는 모양과\nclass 변수의 수준 간의 매핑을 보여주는 범례가 함께\n제공됩니다.' width=384}\n:::\n:::\n\n\n`class`가 `shape`에 매핑되면 두 가지 경고가 표시됩니다.\n\n```text\n1: The shape palette can deal with a maximum of 6 discrete values because more than 6 becomes difficult to discriminate; you have 7. Consider specifying shapes manually if you must have them.\n2: Removed 62 rows containing missing values (`geom_point()`).\n```\n\nggplot2는 기본적으로 한 번에 모양을 6개만 사용하므로 추가 그룹은 그래프되지 않습니다. 데이터셋의 모든 데이터가 그래프에 표현되지 않습니다.\n\n`class`를 `size`나 `alpha` 시각 속성에 매핑할 수도 있습니다. 각각 점의 크기와 투명도를 제어합니다.\n\n\n::: {.cell layout-ncol=\"2\"}\n\n```{.r .cell-code}\n# Left\nggplot(mpg, aes(x = displ, y = hwy, size = class)) +\n  geom_point()\n#> Warning: Using size for a discrete variable is not advised.\n\n# Right\nggplot(mpg, aes(x = displ, y = hwy, alpha = class)) +\n  geom_point()\n#> Warning: Using alpha for a discrete variable is not advised.\n```\n\n::: {.cell-output-display}\n![](ch05_files/figure-html/unnamed-chunk-4-1.png){fig-alt='나란히 있는 두 개의 산점도로, 둘 다 자동차의 고속도로 연료\n효율 대 엔진 크기를 시각화하고 음의\n연관성을 보여줍니다. 왼쪽 그래프에서 class는 크기\n시각 속성에 매핑되어 각 class에 대해 다른 크기를\n나타냅니다. 오른쪽 그래프에서 class는 알파 시각 속성에 매핑되어\n각 class에 대해 다른 알파(투명도) 수준을 나타냅니다.\n각 그래프에는 크기 또는 알파 수준과\nclass 변수의 수준 간의 매핑을 보여주는 범례가 함께\n제공됩니다.' width=384}\n:::\n\n::: {.cell-output-display}\n![](ch05_files/figure-html/unnamed-chunk-4-2.png){fig-alt='나란히 있는 두 개의 산점도로, 둘 다 자동차의 고속도로 연료\n효율 대 엔진 크기를 시각화하고 음의\n연관성을 보여줍니다. 왼쪽 그래프에서 class는 크기\n시각 속성에 매핑되어 각 class에 대해 다른 크기를\n나타냅니다. 오른쪽 그래프에서 class는 알파 시각 속성에 매핑되어\n각 class에 대해 다른 알파(투명도) 수준을 나타냅니다.\n각 그래프에는 크기 또는 알파 수준과\nclass 변수의 수준 간의 매핑을 보여주는 범례가 함께\n제공됩니다.' width=384}\n:::\n:::\n\n\n이 두 가지 모두 경고를 생성합니다.\n\n```text\nUsing alpha for a discrete variable is not advised.\n```\n\n순서가 없는 이산형(범주형) 변수(`class`)를 순서가 있는 시각 속성(`size` 또는 `alpha`)에 매핑하면 실제로 존재하지 않는 순위(ranking)를 암시하므로 바람직하지 않습니다.\n\n시각 속성을 매핑하면 나머지는 ggplot2가 처리합니다. 알맞은 스케일(scale)을 선택하고 수준(levels)과 값(values)의 매핑을 설명하는 범례(legend)를 구성합니다. `x`와 `y` 시각 속성에는 범례 대신 눈금 마크(tick marks)와 라벨이 있는 축 선(axis line)을 만듭니다. 축 선은 범례처럼 위치와 값의 매핑을 설명합니다.\n\n변수 매핑에 의존하지 않고 `aes()` 밖에서 도형 함수의 인수로 시각적 속성을 직접 설정할 수도 있습니다. 예를 들어 그래프의 모든 점을 파란색으로 만듭니다.\n\n\n::: {.cell}\n\n```{.r .cell-code}\nggplot(mpg, aes(x = displ, y = hwy)) +\n  geom_point(color = \"blue\")\n```\n\n::: {.cell-output-display}\n![](ch05_files/figure-html/unnamed-chunk-5-1.png){fig-alt='음의 연관성을 보여주는 자동차의 고속도로 연료 효율 대 엔진 크기의\n산점도. 모든 점은 파란색입니다.' width=576}\n:::\n:::\n\n\n여기서 색상은 변수 정보를 전달하지 않고 그래프의 모양만 바꿉니다. 해당 시각 속성에 알맞은 값을 선택해야 합니다.\n\n- 문자열로 된 색상 이름(`color = \"blue\"`)\n- `mm` 단위의 점 크기(`size = 1`)\n- 숫자로 된 점 모양(`shape = 1`)\n\n\n::: {.cell layout-align=\"center\"}\n::: {.cell-output-display}\n![R에는 숫자로 식별되는 26개의 내장 모양이 있습니다. 일부\n중복되어 보이는 것들이 있습니다. 예를 들어 0, 15, 22는 모두 정사각형입니다.\n차이는 `color`와 `fill` 시각 속성의 상호 작용에서 비롯됩니다.\n속이 빈 모양(0~14)은 `color`에 의해 테두리가 결정되고\n속이 찬 모양(15~20)은 `color`로 채워지며 채워진 모양\n(21~25)은 `color`의 테두리를 갖고 `fill`로 채워집니다. 비슷한 모양끼리\n옆에 있도록 배열되어 있습니다.\n](ch05_files/figure-html/fig-shapes-1.png){#fig-shapes fig-align='center' fig-alt='모양과 이를 나타내는 숫자 간의 매핑:\n0 - 속이 빈 정사각형, 1 - 속이 빈 원,\n2 - 속이 빈 삼각형, 3 - 플러스,\n4 - 십자가, 5 - 속이 빈 다이아몬드,\n6 - 속이 빈 역삼각형, 7 - 정사각형 십자가,\n8 - 별표, 9 - 다이아몬드 플러스,\n10 - 원 플러스, 11 - 별, 12 - 정사각형 플러스,\n13 - 원 십자가, 14 - 정사각형 삼각형, 15 - 정사각형,\n16 - 작은 원, 17 - 삼각형, 18 - 다이아몬드,\n19 - 원, 20 - 총알, 21 - 채워진 원,\n22 - 채워진 정사각형, 23 - 채워진 다이아몬드,\n24 - 채워진 삼각형, 25 - 채워진 역삼각형.' width=576}\n:::\n:::\n\n\n[@fig-shapes]에서 점 객체를 매핑하거나 설정할 수 있는 시각 속성을 살펴봤습니다. 가능한 모든 시각 속성은 [https://ggplot2.tidyverse.org/articles/ggplot2-specs.html](https://ggplot2.tidyverse.org/articles/ggplot2-specs.html)의 시각 속성 사양 비넷(aesthetic specifications vignette, 관련 문서)에서 확인하세요.\n\n그래프에서 쓸 수 있는 시각 속성은 데이터를 나타내는 도형에 따라 다릅니다.\n\n### 연습 문제\n\n1. 점이 분홍색으로 채워진 삼각형인 `hwy` 대 `displ`의 산점도를 만드세요.\n\n2. 다음 코드가 파란색 점이 있는 그래프을 생성하지 않은 이유는 무엇입니까?\n\n\n::: {.cell}\n\n```{.r .cell-code}\nggplot(mpg) +\n  geom_point(aes(x = displ, y = hwy, color = \"blue\"))\n```\n:::\n\n\n3. `stroke` 시각 속성은 무엇을 하나요? 어떤 모양과 함께 작동하나요? (힌트: `?geom_point` 사용)\n\n4. 변수 이름이 아닌 다른 것, 예를 들어 `aes(color = displ < 5)`와 같이 시각 속성을 매핑하면 어떻게 됩니까? 참고로, `x`와 `y`도 지정해야 합니다.\n\n## 도형\n\n이 두 그래프은 어떻게 비슷한가요?\n\n\n::: {.cell layout-ncol=\"2\"}\n::: {.cell-output-display}\n![](ch05_files/figure-html/unnamed-chunk-7-1.png){fig-alt='두 개의 그래프이 있습니다. 왼쪽 그래프은 자동차의 엔진 크기 대 고속도로\n연료 효율의 산점도이고 오른쪽 그래프은\n이들 변수 간 관계의 궤적을 따르는 매끄러운\n곡선을 보여줍니다. 매끄러운 곡선 주변의 신뢰 구간도\n표시됩니다.' width=288}\n:::\n\n::: {.cell-output-display}\n![](ch05_files/figure-html/unnamed-chunk-7-2.png){fig-alt='두 개의 그래프이 있습니다. 왼쪽 그래프은 자동차의 엔진 크기 대 고속도로\n연료 효율의 산점도이고 오른쪽 그래프은\n이들 변수 간 관계의 궤적을 따르는 매끄러운\n곡선을 보여줍니다. 매끄러운 곡선 주변의 신뢰 구간도\n표시됩니다.' width=288}\n:::\n:::\n\n\n두 그래프은 동일한 `x` 변수와 `y` 변수를 포함하며 같은 데이터를 설명합니다. 하지만 서로 같은 그래프은 아닙니다. 데이터를 나타내는 양식(geometric object), 즉 도형(geom)이 다릅니다. 왼쪽 그래프은 점 도형을, 오른쪽 그래프은 데이터에 적합된(fitted) 매끄러운(smooth) 선 도형을 사용합니다.\n\n그래프의 도형을 바꾸려면 `ggplot()`의 도형 함수를 변경하세요.\n\n\n::: {.cell}\n\n```{.r .cell-code}\n# Left\nggplot(mpg, aes(x = displ, y = hwy)) +\n  geom_point()\n\n# Right\nggplot(mpg, aes(x = displ, y = hwy)) +\n  geom_smooth()\n#> `geom_smooth()` using method = 'loess' and formula = 'y ~ x'\n```\n:::\n\n\nggplot2의 모든 도형 함수는 `mapping` 인수를 사용합니다. 이 인수는 도형 레이어에 정의하거나 `ggplot()` 레이어에 전역으로 정의합니다. 다만 모든 시각 속성이 모든 도형에서 작동하지는 않습니다.\n\n점의 모양은 설정하지만 선의 \"모양(shape)\"은 설정하지 못합니다. 시도해도 ggplot2는 해당 시각 속성을 조용히 무시합니다. 반면 선종류(linetype)는 설정할 수 있습니다.\n\n`geom_smooth()`는 선종류에 매핑한 변수의 고유 값마다 다른 선을 그립니다.\n\n\n::: {.cell layout-ncol=\"2\"}\n\n```{.r .cell-code}\n# Left\nggplot(mpg, aes(x = displ, y = hwy, shape = drv)) +\n  geom_smooth()\n\n# Right\nggplot(mpg, aes(x = displ, y = hwy, linetype = drv)) +\n  geom_smooth()\n```\n\n::: {.cell-output-display}\n![](ch05_files/figure-html/unnamed-chunk-9-1.png){fig-alt='자동차의 고속도로 연료 효율 대 엔진 크기에 대한 두 개의 그래프.\n데이터는 매끄러운 곡선으로 나타납니다. 왼쪽에는 선종류가 모두 같은\n세 개의 매끄러운 곡선이 있습니다. 오른쪽에는 각 구동계(drive train) 유형에 대해\n선종류(실선, 파선 또는 긴 파선)가 다른 세 개의 매끄러운 곡선이\n있습니다. 두 그래프 모두에서 매끄러운 곡선 주위의 신뢰\n구간도 표시됩니다.' width=288}\n:::\n\n::: {.cell-output-display}\n![](ch05_files/figure-html/unnamed-chunk-9-2.png){fig-alt='자동차의 고속도로 연료 효율 대 엔진 크기에 대한 두 개의 그래프.\n데이터는 매끄러운 곡선으로 나타납니다. 왼쪽에는 선종류가 모두 같은\n세 개의 매끄러운 곡선이 있습니다. 오른쪽에는 각 구동계(drive train) 유형에 대해\n선종류(실선, 파선 또는 긴 파선)가 다른 세 개의 매끄러운 곡선이\n있습니다. 두 그래프 모두에서 매끄러운 곡선 주위의 신뢰\n구간도 표시됩니다.' width=288}\n:::\n:::\n\n\n`geom_smooth()`는 자동차의 구동계(drive train)를 나타내는 `drv` 값에 따라 자동차를 세 선으로 나눕니다. 각 선은 각각 `4`, `f`, `r` 값을 가진 모든 점을 설명합니다.\n\n여기서 `4`는 4륜 구동(four-wheel drive), `f`는 전륜 구동(front-wheel drive), `r`은 후륜 구동(rear-wheel drive)을 의미합니다.\n\n이 설명이 낯설다면 원시 데이터(raw data) 위에 선을 오버레이(overlaying)하고 `drv`에 따라 모두 색칠하면 더 명확해집니다.\n\n\n::: {.cell}\n\n```{.r .cell-code}\nggplot(mpg, aes(x = displ, y = hwy, color = drv)) +\n  geom_point() +\n  geom_smooth(aes(linetype = drv))\n```\n\n::: {.cell-output-display}\n![](ch05_files/figure-html/unnamed-chunk-10-1.png){fig-alt='자동차의 고속도로 연료 효율 대 엔진 크기 그래프. 데이터는\n점(구동계별로 색칠됨)뿐만 아니라 매끄러운 곡선(선종류 또한\n구동계에 따라 결정됨)으로 나타납니다.\n매끄러운 곡선 주위의 신뢰 구간도 표시됩니다.' width=576}\n:::\n:::\n\n\n이 그래프에는 한 그래프에 도형이 두 개 들어 있다는 점에 유의하세요.\n\n`geom_smooth()`와 같은 많은 도형은 여러 데이터 행을 단일 도형로 표시합니다. 여러 객체를 그리려면 범주형 변수에 `group` 시각 속성을 설정합니다. ggplot2는 그룹화 변수의 고유 값마다 별도의 객체를 그립니다. 이산형 변수를 시각 속성에 매핑하면 위의 `linetype` 예제처럼 이러한 도형의 데이터를 자동으로 그룹화합니다. `group` 시각 속성 자체는 도형에 범례나 구별되는 특징(distinguishing features)을 추가하지 않으므로 이 기능을 활용하기 편리합니다.\n\n\n::: {.cell layout-ncol=\"3\"}\n\n```{.r .cell-code}\n# Left\nggplot(mpg, aes(x = displ, y = hwy)) +\n  geom_smooth()\n\n# Middle\nggplot(mpg, aes(x = displ, y = hwy)) +\n  geom_smooth(aes(group = drv))\n\n# Right\nggplot(mpg, aes(x = displ, y = hwy)) +\n  geom_smooth(aes(color = drv), show.legend = FALSE)\n```\n\n::: {.cell-output-display}\n![](ch05_files/figure-html/unnamed-chunk-11-1.png){fig-alt='세 개의 그래프, 각각 y축은 고속도로 연료 효율이고 x축은 자동차의 엔진 크기이며\n데이터는 매끄러운 곡선으로 표현됩니다.\n첫 번째 그래프에는 이 두 변수만 있고 가운데 그래프에는 구동계의\n각 수준에 대해 3개의 별도 매끄러운 곡선이 있으며 오른쪽\n그래프은 구동계의 각 수준에 대해 동일하게 3개의 별도 매끄러운 곡선이\n있을 뿐만 아니라 곡선들이 다른 색상으로 그려집니다.\n매끄러운 곡선 주위의 신뢰 구간\n또한 표시됩니다.' width=288}\n:::\n\n::: {.cell-output-display}\n![](ch05_files/figure-html/unnamed-chunk-11-2.png){fig-alt='세 개의 그래프, 각각 y축은 고속도로 연료 효율이고 x축은 자동차의 엔진 크기이며\n데이터는 매끄러운 곡선으로 표현됩니다.\n첫 번째 그래프에는 이 두 변수만 있고 가운데 그래프에는 구동계의\n각 수준에 대해 3개의 별도 매끄러운 곡선이 있으며 오른쪽\n그래프은 구동계의 각 수준에 대해 동일하게 3개의 별도 매끄러운 곡선이\n있을 뿐만 아니라 곡선들이 다른 색상으로 그려집니다.\n매끄러운 곡선 주위의 신뢰 구간\n또한 표시됩니다.' width=288}\n:::\n\n::: {.cell-output-display}\n![](ch05_files/figure-html/unnamed-chunk-11-3.png){fig-alt='세 개의 그래프, 각각 y축은 고속도로 연료 효율이고 x축은 자동차의 엔진 크기이며\n데이터는 매끄러운 곡선으로 표현됩니다.\n첫 번째 그래프에는 이 두 변수만 있고 가운데 그래프에는 구동계의\n각 수준에 대해 3개의 별도 매끄러운 곡선이 있으며 오른쪽\n그래프은 구동계의 각 수준에 대해 동일하게 3개의 별도 매끄러운 곡선이\n있을 뿐만 아니라 곡선들이 다른 색상으로 그려집니다.\n매끄러운 곡선 주위의 신뢰 구간\n또한 표시됩니다.' width=288}\n:::\n:::\n\n\n도형 함수를 배치하면 ggplot2는 이를 해당 레이어의 지역 매핑으로 취급합니다. 지역 매핑은 그 레이어에서는 전역 매핑을 확장하거나 덮어씁니다(overwrite). 따라서 레이어마다 서로 다른 시각 속성을 표시합니다.\n\n\n::: {.cell}\n\n```{.r .cell-code}\nggplot(mpg, aes(x = displ, y = hwy)) +\n  geom_point(aes(color = class)) +\n  geom_smooth()\n```\n\n::: {.cell-output-display}\n![](ch05_files/figure-html/unnamed-chunk-12-1.png){fig-alt='자동차의 고속도로 연료 효율 대 엔진 크기 산점도, 점은\n자동차 class에 따라 색칠됩니다. 자동차의 고속도로 연료 효율 대 엔진 크기 간\n관계의 궤적을 따르는 매끄러운 곡선이 주위에\n신뢰 구간과 함께 겹쳐집니다.' width=576}\n:::\n:::\n\n\n같은 방식으로 각 레이어에 서로 다른 `data`를 지정할 수도 있습니다. 여기서는 2인승(two-seater) 자동차를 강조하려고 빨간색 점과 속이 빈 원을 사용합니다. `geom_point()`의 로컬 데이터 인수는 해당 레이어에서만 `ggplot()`의 전역 데이터를 무효화(overrides)합니다.\n\n\n::: {.cell}\n\n```{.r .cell-code}\nggplot(mpg, aes(x = displ, y = hwy)) +\n  geom_point() +\n  geom_point(\n    data = mpg |> filter(class == \"2seater\"),\n    color = \"red\"\n  ) +\n  geom_point(\n    data = mpg |> filter(class == \"2seater\"),\n    shape = \"circle open\", size = 3, color = \"red\"\n  )\n```\n\n::: {.cell-output-display}\n![](ch05_files/figure-html/unnamed-chunk-13-1.png){fig-alt='자동차의 고속도로 연료 효율 대 엔진 크기 산점도, 여기서\n2인승 자동차는 빨간색 점과 속이 빈 원으로 강조 표시됩니다.' width=576}\n:::\n:::\n\n\n도형은 ggplot2의 기본 구성 요소(building blocks)입니다. 도형을 바꾸면 그래프의 모양(look)이 완전히 달라지고 데이터의 다른 특징이 드러납니다.\n\n아래의 히스토그램과 밀도 그래프(density plot)에서는 고속도로 주행 거리(highway mileage)의 분포가 쌍봉형(bimodal)이고 오른쪽으로 꼬리가 긴 형태(right skewed)입니다. 상자 그림(boxplot)에는 잠재적인 이상치(outliers)가 두 개 나타납니다.\n\n\n::: {.cell layout-ncol=\"3\"}\n\n```{.r .cell-code}\n# Left\nggplot(mpg, aes(x = hwy)) +\n  geom_histogram(binwidth = 2)\n\n# Middle\nggplot(mpg, aes(x = hwy)) +\n  geom_density()\n\n# Right\nggplot(mpg, aes(x = hwy)) +\n  geom_boxplot()\n```\n\n::: {.cell-output-display}\n![](ch05_files/figure-html/unnamed-chunk-14-1.png){fig-alt='세 개의 그래프: 고속도로 주행 거리의 히스토그램,\n밀도 그래프, 상자 그림.' width=288}\n:::\n\n::: {.cell-output-display}\n![](ch05_files/figure-html/unnamed-chunk-14-2.png){fig-alt='세 개의 그래프: 고속도로 주행 거리의 히스토그램,\n밀도 그래프, 상자 그림.' width=288}\n:::\n\n::: {.cell-output-display}\n![](ch05_files/figure-html/unnamed-chunk-14-3.png){fig-alt='세 개의 그래프: 고속도로 주행 거리의 히스토그램,\n밀도 그래프, 상자 그림.' width=288}\n:::\n:::\n\n\n다른 도형이 필요하면 먼저 확장 패키지(extension packages)부터 찾아보세요(샘플링은 [https://exts.ggplot2.tidyverse.org/gallery/](https://exts.ggplot2.tidyverse.org/gallery/) 참조).\n\n예를 들어 ggridges 패키지([https://wilkelab.org/ggridges](https://wilkelab.org/ggridges/))는 릿지라인 그래프(ridgeline plots)을 만듭니다. 이 그래프은 범주형 변수의 여러 수준에서 숫자형 변수의 밀도를 시각화할 때 유용합니다.\n\n다음 그래프에서는 새로운 도형(`geom_density_ridges()`)을 사용합니다. 동일한 변수 `drv`를 `y`, `fill`, `color`에 매핑하고 밀도 곡선을 투명하게 하려고 `alpha = 0.5`도 설정했습니다.\n\n\n::: {.cell}\n\n```{.r .cell-code}\nlibrary(ggridges)\n\nggplot(mpg, aes(x = hwy, y = drv, fill = drv, color = drv)) +\n  geom_density_ridges(alpha = 0.5, show.legend = FALSE)\n#> Picking joint bandwidth of 1.28\n```\n\n::: {.cell-output-display}\n![](ch05_files/figure-html/unnamed-chunk-15-1.png){fig-alt='후륜(rear wheel), 전륜(front wheel), 4륜(4-wheel drive) 구동 자동차에 대한 고속도로 주행 거리의 밀도 곡선이 따로 그래프됩니다. 후륜 및 4륜 구동 자동차의 분포는 쌍봉형이고 대략 대칭적인 반면 전륜 구동 자동차는 단봉형(unimodal)이고 오른쪽으로 꼬리가 깁니다.' width=576}\n:::\n:::\n\n\nggplot2의 모든 도형과 패키지 함수를 포괄적으로 보려면 참조 페이지 [https://ggplot2.tidyverse.org/reference](https://ggplot2.tidyverse.org/reference)를 이용하세요. 단일 도형은 도움말(`?geom_smooth`)에서 자세히 알아볼 수 있습니다.\n\n### 연습 문제\n\n1. 선 차트(line chart)를 그리려면 어떤 도형을 사용하시겠습니까? 상자 그림(boxplot)은요? 히스토그램(histogram)은요? 영역 차트(area chart)는요?\n\n2. 이 장의 앞부분에서 우리는 설명 없이 `show.legend`를 사용했습니다. 여기서 `show.legend = FALSE`는 무슨 역할을 하나요? 제거하면 어떻게 되나요? 앞에서 왜 그것을 사용했다고 생각하시나요?\n\n\n::: {.cell}\n\n```{.r .cell-code}\nggplot(mpg, aes(x = displ, y = hwy)) +\n  geom_smooth(aes(color = drv), show.legend = FALSE)\n```\n:::\n\n\n3. `geom_smooth()`에 대한 `se` 인수는 무슨 역할을 하나요?\n\n4. 다음 그래프를 생성하는 데 필요한 R 코드를 재현(Recreate)하세요. 그래프에 범주형 변수가 사용될 때마다 그것은 `drv`입니다.\n\n\n::: {.cell layout-ncol=\"2\"}\n::: {.cell-output-display}\n![](ch05_files/figure-html/unnamed-chunk-17-1.png){fig-alt='이 그림에는 3x2 그리드로 배열된 6개의 산점도가 있습니다.\n모든 그래프에서 자동차의 고속도로 연료 효율이 y축에 있고\n엔진 크기가 x축에 있습니다. 첫 번째 그래프은 매끄러운 곡선이 겹쳐진\n모든 검은색 점을 보여줍니다. 두 번째 그래프에서도 점들은\n모두 검은색이며 구동계의 각 수준에 대해 별도의 매끄러운 곡선이\n겹쳐져 있습니다. 세 번째 그래프에서 점과 매끄러운 곡선은\n구동계의 각 수준에 대해 다른 색상으로 표시됩니다. 네 번째\n그래프에서 점들은 구동계의 각 수준에 대해 다른 색상으로\n표시되지만 전체 데이터에 맞춰진 단일한 매끄러운 선만\n있습니다. 다섯 번째 그래프에서 점들은 구동계의 각 수준에\n대해 다른 색상으로 표시되며 선종류가 다른\n별도의 매끄러운 곡선이 구동계의 각 수준에 맞춰집니다. 그리고\n마지막으로 여섯 번째 그래프에서 점들은 구동계의 각 수준에\n대해 다른 색상으로 표시되며 두꺼운 흰색 테두리를 가집니다.' width=288}\n:::\n\n::: {.cell-output-display}\n![](ch05_files/figure-html/unnamed-chunk-17-2.png){fig-alt='이 그림에는 3x2 그리드로 배열된 6개의 산점도가 있습니다.\n모든 그래프에서 자동차의 고속도로 연료 효율이 y축에 있고\n엔진 크기가 x축에 있습니다. 첫 번째 그래프은 매끄러운 곡선이 겹쳐진\n모든 검은색 점을 보여줍니다. 두 번째 그래프에서도 점들은\n모두 검은색이며 구동계의 각 수준에 대해 별도의 매끄러운 곡선이\n겹쳐져 있습니다. 세 번째 그래프에서 점과 매끄러운 곡선은\n구동계의 각 수준에 대해 다른 색상으로 표시됩니다. 네 번째\n그래프에서 점들은 구동계의 각 수준에 대해 다른 색상으로\n표시되지만 전체 데이터에 맞춰진 단일한 매끄러운 선만\n있습니다. 다섯 번째 그래프에서 점들은 구동계의 각 수준에\n대해 다른 색상으로 표시되며 선종류가 다른\n별도의 매끄러운 곡선이 구동계의 각 수준에 맞춰집니다. 그리고\n마지막으로 여섯 번째 그래프에서 점들은 구동계의 각 수준에\n대해 다른 색상으로 표시되며 두꺼운 흰색 테두리를 가집니다.' width=288}\n:::\n\n::: {.cell-output-display}\n![](ch05_files/figure-html/unnamed-chunk-17-3.png){fig-alt='이 그림에는 3x2 그리드로 배열된 6개의 산점도가 있습니다.\n모든 그래프에서 자동차의 고속도로 연료 효율이 y축에 있고\n엔진 크기가 x축에 있습니다. 첫 번째 그래프은 매끄러운 곡선이 겹쳐진\n모든 검은색 점을 보여줍니다. 두 번째 그래프에서도 점들은\n모두 검은색이며 구동계의 각 수준에 대해 별도의 매끄러운 곡선이\n겹쳐져 있습니다. 세 번째 그래프에서 점과 매끄러운 곡선은\n구동계의 각 수준에 대해 다른 색상으로 표시됩니다. 네 번째\n그래프에서 점들은 구동계의 각 수준에 대해 다른 색상으로\n표시되지만 전체 데이터에 맞춰진 단일한 매끄러운 선만\n있습니다. 다섯 번째 그래프에서 점들은 구동계의 각 수준에\n대해 다른 색상으로 표시되며 선종류가 다른\n별도의 매끄러운 곡선이 구동계의 각 수준에 맞춰집니다. 그리고\n마지막으로 여섯 번째 그래프에서 점들은 구동계의 각 수준에\n대해 다른 색상으로 표시되며 두꺼운 흰색 테두리를 가집니다.' width=288}\n:::\n\n::: {.cell-output-display}\n![](ch05_files/figure-html/unnamed-chunk-17-4.png){fig-alt='이 그림에는 3x2 그리드로 배열된 6개의 산점도가 있습니다.\n모든 그래프에서 자동차의 고속도로 연료 효율이 y축에 있고\n엔진 크기가 x축에 있습니다. 첫 번째 그래프은 매끄러운 곡선이 겹쳐진\n모든 검은색 점을 보여줍니다. 두 번째 그래프에서도 점들은\n모두 검은색이며 구동계의 각 수준에 대해 별도의 매끄러운 곡선이\n겹쳐져 있습니다. 세 번째 그래프에서 점과 매끄러운 곡선은\n구동계의 각 수준에 대해 다른 색상으로 표시됩니다. 네 번째\n그래프에서 점들은 구동계의 각 수준에 대해 다른 색상으로\n표시되지만 전체 데이터에 맞춰진 단일한 매끄러운 선만\n있습니다. 다섯 번째 그래프에서 점들은 구동계의 각 수준에\n대해 다른 색상으로 표시되며 선종류가 다른\n별도의 매끄러운 곡선이 구동계의 각 수준에 맞춰집니다. 그리고\n마지막으로 여섯 번째 그래프에서 점들은 구동계의 각 수준에\n대해 다른 색상으로 표시되며 두꺼운 흰색 테두리를 가집니다.' width=288}\n:::\n\n::: {.cell-output-display}\n![](ch05_files/figure-html/unnamed-chunk-17-5.png){fig-alt='이 그림에는 3x2 그리드로 배열된 6개의 산점도가 있습니다.\n모든 그래프에서 자동차의 고속도로 연료 효율이 y축에 있고\n엔진 크기가 x축에 있습니다. 첫 번째 그래프은 매끄러운 곡선이 겹쳐진\n모든 검은색 점을 보여줍니다. 두 번째 그래프에서도 점들은\n모두 검은색이며 구동계의 각 수준에 대해 별도의 매끄러운 곡선이\n겹쳐져 있습니다. 세 번째 그래프에서 점과 매끄러운 곡선은\n구동계의 각 수준에 대해 다른 색상으로 표시됩니다. 네 번째\n그래프에서 점들은 구동계의 각 수준에 대해 다른 색상으로\n표시되지만 전체 데이터에 맞춰진 단일한 매끄러운 선만\n있습니다. 다섯 번째 그래프에서 점들은 구동계의 각 수준에\n대해 다른 색상으로 표시되며 선종류가 다른\n별도의 매끄러운 곡선이 구동계의 각 수준에 맞춰집니다. 그리고\n마지막으로 여섯 번째 그래프에서 점들은 구동계의 각 수준에\n대해 다른 색상으로 표시되며 두꺼운 흰색 테두리를 가집니다.' width=288}\n:::\n\n::: {.cell-output-display}\n![](ch05_files/figure-html/unnamed-chunk-17-6.png){fig-alt='이 그림에는 3x2 그리드로 배열된 6개의 산점도가 있습니다.\n모든 그래프에서 자동차의 고속도로 연료 효율이 y축에 있고\n엔진 크기가 x축에 있습니다. 첫 번째 그래프은 매끄러운 곡선이 겹쳐진\n모든 검은색 점을 보여줍니다. 두 번째 그래프에서도 점들은\n모두 검은색이며 구동계의 각 수준에 대해 별도의 매끄러운 곡선이\n겹쳐져 있습니다. 세 번째 그래프에서 점과 매끄러운 곡선은\n구동계의 각 수준에 대해 다른 색상으로 표시됩니다. 네 번째\n그래프에서 점들은 구동계의 각 수준에 대해 다른 색상으로\n표시되지만 전체 데이터에 맞춰진 단일한 매끄러운 선만\n있습니다. 다섯 번째 그래프에서 점들은 구동계의 각 수준에\n대해 다른 색상으로 표시되며 선종류가 다른\n별도의 매끄러운 곡선이 구동계의 각 수준에 맞춰집니다. 그리고\n마지막으로 여섯 번째 그래프에서 점들은 구동계의 각 수준에\n대해 다른 색상으로 표시되며 두꺼운 흰색 테두리를 가집니다.' width=288}\n:::\n:::\n\n\n## 분할\n\n`facet_wrap()`을 사용한 분할(faceting)은 범주형 변수를 기준으로 그래프을 하위 그래프(subplots)으로 나눕니다. 각 하위 그래프은 데이터의 부분 집합(subset) 하나를 표시합니다.\n\n\n::: {.cell}\n\n```{.r .cell-code}\nggplot(mpg, aes(x = displ, y = hwy)) +\n  geom_point() +\n  facet_wrap(~cyl)\n```\n\n::: {.cell-output-display}\n![](ch05_files/figure-html/unnamed-chunk-18-1.png){fig-alt='자동차의 고속도로 연료 효율 대 엔진 크기 산점도, 실린더 수별로\n분할 처리(faceted)되었으며 두 개의 행에 걸쳐 분할이 표시됩니다.' width=576}\n:::\n:::\n\n\n두 변수의 조합(combination)으로 그래프을 분할 처리하려면 `facet_wrap()` 대신 `facet_grid()`를 사용하세요. `facet_grid()`의 첫 번째 인수도 공식(formula)이지만 이번에는 `rows ~ cols` 형태의 양면(double sided) 공식입니다.\n\n\n::: {.cell}\n\n```{.r .cell-code}\nggplot(mpg, aes(x = displ, y = hwy)) +\n  geom_point() +\n  facet_grid(drv ~ cyl)\n```\n\n::: {.cell-output-display}\n![](ch05_files/figure-html/unnamed-chunk-19-1.png){fig-alt='자동차의 고속도로 연료 효율 대 엔진 크기 산점도, 행에 걸쳐\n실린더 수로 분할 처리되고 열에 걸쳐 구동계 유형으로\n분할 처리되었습니다. 이로 인해 12개의 분할으로 구성된 4x3 그리드가 생성됩니다.\n이들 분할 중 일부에는 관측치가 없습니다. 5기통 4륜 구동,\n4기통 또는 5기통 전륜 구동,\n4기통 또는 5기통 전륜 구동.' width=576}\n:::\n:::\n\n\n기본적으로 모든 분할은 x축과 y축의 스케일과 범위를 공유합니다. 분할 전체의 데이터를 비교할 때는 유용하지만 각 분할 안의 관계를 시각화할 때는 제약이 될 수 있습니다.\n\n분할 함수의 `scales` 인수를 `\"free_x\"`로 설정하면 열마다 x축 스케일이 달라집니다. `\"free_y\"`는 행마다 y축 스케일을 다르게 하고 `\"free\"`는 둘 다 허용합니다.\n\n\n::: {.cell}\n\n```{.r .cell-code}\nggplot(mpg, aes(x = displ, y = hwy)) +\n  geom_point() +\n  facet_grid(drv ~ cyl, scales = \"free\")\n```\n\n::: {.cell-output-display}\n![](ch05_files/figure-html/unnamed-chunk-20-1.png){fig-alt='자동차의 고속도로 연료 효율 대 엔진 크기 산점도, 행에 걸쳐\n실린더 수로 분할 처리되고 열에 걸쳐 구동계 유형으로\n분할 처리되었습니다. 이로 인해 12개의 분할으로 구성된 4x3 그리드가 생성됩니다.\n이들 분할 중 일부에는 관측치가 없습니다. 5기통 4륜 구동,\n4기통 또는 5기통 전륜 구동. 행 내의 분할들은\n동일한 y-스케일을 공유하고 열 내의 분할들은\n동일한 x-스케일을 공유합니다.' width=576}\n:::\n:::\n\n\n### 연습 문제\n\n1. 연속형 변수에 대해 분할 처리하면 어떻게 되나요?\n\n2. 위의 그래프에서 `facet_grid(drv ~ cyl)`를 사용했을 때 빈 셀들은 무엇을 의미하나요? 다음 코드를 실행하세요. 결과 그래프과 어떤 관련이 있나요?\n\n\n::: {.cell}\n\n```{.r .cell-code}\nggplot(mpg) +\n  geom_point(aes(x = drv, y = cyl))\n```\n:::\n\n\n3. 다음 코드는 어떤 그래프을 만드나요? `.`은 무슨 역할을 하나요?\n\n\n::: {.cell}\n\n```{.r .cell-code}\nggplot(mpg) +\n  geom_point(aes(x = displ, y = hwy)) +\n  facet_grid(drv ~ .)\n\nggplot(mpg) +\n  geom_point(aes(x = displ, y = hwy)) +\n  facet_grid(. ~ cyl)\n```\n:::\n\n\n4. 이 섹션의 첫 번째 분할 그래프을 살펴봅시다. 색상 시각 속성 대신 분할 처리를 사용할 때의 이점은 무엇입니까? 단점은 무엇입니까? 데이터셋이 더 커진다면 그 균형이 어떻게 변할까요?\n\n\n::: {.cell}\n\n```{.r .cell-code}\nggplot(mpg) +\n  geom_point(aes(x = displ, y = hwy)) +\n  facet_wrap(~ cyl, nrow = 2)\n```\n:::\n\n\n5. `?facet_wrap`을 읽어보세요. `nrow`는 무슨 역할을 하나요? `ncol`은 무슨 역할을 하나요? 개별 패널의 레이아웃을 제어하는 다른 옵션은 무엇입니까? `facet_grid()`에는 왜 `nrow` 및 `ncol` 인수가 없습니까?\n\n6. 다음 그래프 중 어느 것이 서로 다른 구동계를 가진 자동차 간에 엔진 크기(`displ`)를 비교하기 더 쉽게 만드나요? 이것은 분할 변수를 행에 배치할지 열에 배치할지 결정하는 데 대해 무엇을 시사합니까?\n\n\n::: {.cell}\n\n```{.r .cell-code}\nggplot(mpg, aes(x = displ)) +\n  geom_histogram() +\n  facet_grid(drv ~ .)\n\nggplot(mpg, aes(x = displ)) +\n  geom_histogram() +\n  facet_grid(. ~ drv)\n```\n:::\n\n\n7. `facet_grid()` 대신 `facet_wrap()`을 사용하여 다음 그래프을 다시 만들어보세요. 분할 라벨의 위치는 어떻게 변경되나요?\n\n\n::: {.cell}\n\n```{.r .cell-code}\nggplot(mpg) +\n  geom_point(aes(x = displ, y = hwy)) +\n  facet_grid(drv ~ .)\n```\n:::\n\n\n## 통계적 변환\n\n`geom_bar()` 또는 `geom_col()`로 그려진 기본적인 막대 차트를 고려해 보세요.\n다음 차트는 `cut`별로 그룹화된 `diamonds` 데이터셋의 총 다이아몬드 수를 표시합니다.\n`diamonds` 데이터셋은 ggplot2 패키지에 있으며 각 다이아몬드의 `price`, `carat`, `color`, `clarity`, `cut`을 포함하여 약 54,000개의 다이아몬드에 대한 정보가 들어 있습니다.\n\n차트를 보면 저품질 컷보다 고품질 컷 다이아몬드가 더 많습니다.\n\n\n::: {.cell}\n\n```{.r .cell-code}\nggplot(diamonds, aes(x = cut)) +\n  geom_bar()\n```\n\n::: {.cell-output-display}\n![](ch05_files/figure-html/unnamed-chunk-26-1.png){fig-alt='각 다이아몬드 컷의 개수를 나타내는 막대 차트. 약 1500개의\nFair, 5000개의 Good, 12000개의 Very Good, 14000개의 Premium, 22000개의 Ideal 컷\n다이아몬드가 있습니다.' width=576}\n:::\n:::\n\n\nx축에는 `diamonds`의 변수인 `cut`이 표시됩니다. y축에는 개수(count)가 표시되지만 개수는 `diamonds`의 변수가 아닙니다! 어디에서 온 값일까요? 산점도처럼 데이터셋의 원시 값(raw values)을 그래프하는 그래프가 많지만 막대 차트처럼 새 값을 계산하는 그래프도 있습니다.\n\n1. 막대 차트, 히스토그램 및 빈도 다각형(frequency polygons)은 데이터를 구간(bin)으로 나누고 각 구간에 속하는 점의 수인 구간 개수를 그래프합니다.\n\n2. 스무더(Smoothers)는 데이터에 모델을 적합시킨(fit a model) 다음 모델의 예측값을 그래프합니다.\n\n3. 상자 그림(Boxplots)은 분포의 5가지 요약 수치(five-number summary)를 계산한 다음 그 요약을 특별한 형식의 상자로 표시합니다.\n\n그래프의 새 값을 계산하는 알고리즘을 통계적 변환(statistical transformation)의 줄임말인 stat이라고 합니다. 다음은 이 과정이 `geom_bar()`에서 작동하는 방식입니다.\n\n도형이 사용하는 stat은 `stat` 인수의 기본값에서 확인합니다.\n예를 들어 `?geom_bar`를 보면 `stat`의 기본값은 \"count\"입니다. 곧 `geom_bar()`가 `stat_count()`를 사용한다는 뜻입니다.\n\n`stat_count()`는 `geom_bar()`와 같은 페이지에 문서화되어 있습니다. 아래로 스크롤하면 \"Computed variables(계산된 변수)\" 섹션에서 새 변수 `count`와 `prop`을 계산한다고 설명합니다.\n\n모든 도형에는 기본(default) stat이 있으며 모든 stat에는 기본 도형이 있습니다.\n따라서 일반적으로 기본 통계적 변환을 따로 신경 쓰지 않고 도형을 사용합니다.\n\n하지만 stat을 명시적으로 사용해야 하는 이유가 세 가지 있습니다.\n\n1. 기본 stat을 재정의(override)할 때입니다. 아래 코드에서는 `geom_bar()`의 stat을 개수(count)(기본값)에서 항등(identity)으로 바꿉니다. 그러면 막대 높이를 y 변수의 원시 값에 매핑합니다.\n\n\n::: {.cell}\n\n```{.r .cell-code}\ndiamonds |>\n  count(cut) |>\n  ggplot(aes(x = cut, y = n)) +\n  geom_bar(stat = \"identity\")\n```\n\n::: {.cell-output-display}\n![](ch05_files/figure-html/unnamed-chunk-27-1.png){fig-alt='각 다이아몬드 컷의 개수를 나타내는 막대 차트. 약 1500개의\nFair, 5000개의 Good, 12000개의 Very Good, 14000개의 Premium, 22000개의 Ideal 컷\n다이아몬드가 있습니다.' width=576}\n:::\n:::\n\n\n2. 변환된 변수와 시각 속성의 기본 매핑을 재정의할 때입니다. 예를 들어 개수 대신 비율(proportions) 막대 차트를 표시합니다. stat이 계산하는 변수는 `geom_bar()` 도움말의 \"computed variables\" 섹션에서 찾으세요.\n\n\n::: {.cell}\n\n```{.r .cell-code}\nggplot(diamonds, aes(x = cut, y = after_stat(prop), group = 1)) +\n  geom_bar()\n```\n\n::: {.cell-output-display}\n![](ch05_files/figure-html/unnamed-chunk-28-1.png){fig-alt='각 다이아몬드 컷의 비율을 나타내는 막대 차트. 대략 Fair\n다이아몬드가 0.03, Good 0.09, Very Good 0.22, Premium 0.26,\nIdeal 0.40을 차지합니다.' width=576}\n:::\n:::\n\n\n3. 코드에서 통계적 변환을 강조할 때입니다. 예를 들어 고유한 각 x 값의 y 값을 요약하는 `stat_summary()`를 사용하면 계산할 요약 수치가 눈에 띕니다.\n\n\n::: {.cell}\n\n```{.r .cell-code}\nggplot(diamonds) +\n  stat_summary(\n    aes(x = cut, y = depth),\n    fun.min = min,\n    fun.max = max,\n    fun = median\n  )\n```\n\n::: {.cell-output-display}\n![](ch05_files/figure-html/unnamed-chunk-29-1.png){fig-alt='y축은 depth, x축은 다이아몬드의 cut(수준은\nfair, good, very good, premium, ideal)인 그래프. cut의\n각 수준에 대해 해당 cut 카테고리의 다이아몬드의\n최소 depth에서 최대 depth까지 수직선이 연장되고\n중앙값 depth가 선 위에 점으로 표시됩니다.' width=576}\n:::\n:::\n\n\nggplot2에는 stat이 20개 이상 있습니다. 각 stat은 함수이므로 일반적인 방법(`?stat_bin`)으로 도움말을 볼 수 있습니다.\n\n### 연습 문제\n\n1. `stat_summary()`와 연관된 기본 도형은 무엇인가요? stat 함수 대신 해당 도형 함수를 사용하도록 이전 그래프을 어떻게 다시 작성할 수 있을까요?\n\n2. `geom_col()`은 무슨 역할을 하나요? `geom_bar()`와 어떻게 다른가요?\n\n3. 대부분의 도형과 stat은 거의 항상 함께 사용되는 쌍으로 제공됩니다. 모든 쌍의 목록을 만드세요. 그들은 어떤 공통점이 있나요? (힌트: 문서를 자세히 읽어보세요.)\n\n4. `stat_smooth()`는 어떤 변수들을 계산하나요? 어떤 인수들이 그 동작을 제어하나요?\n\n5. 비율 막대 차트에서 우리는 `group = 1`을 설정해야 했습니다. 왜일까요? 다시 말해 이 두 그래프의 문제는 무엇입니까?\n\n\n::: {.cell}\n\n```{.r .cell-code}\nggplot(diamonds, aes(x = cut, y = after_stat(prop))) +\n  geom_bar()\nggplot(diamonds, aes(x = cut, fill = color, y = after_stat(prop))) +\n  geom_bar()\n```\n:::\n\n\n## 위치 조정\n\n막대 차트에는 또 다른 마법이 있습니다.\n`color` 시각 속성이나 더 유용한 `fill` 시각 속성으로 막대 차트에 색을 입힙니다.\n\n\n::: {.cell layout-ncol=\"2\"}\n\n```{.r .cell-code}\n# Left\nggplot(mpg, aes(x = drv, color = drv)) +\n  geom_bar()\n\n# Right\nggplot(mpg, aes(x = drv, fill = drv)) +\n  geom_bar()\n```\n\n::: {.cell-output-display}\n![](ch05_files/figure-html/unnamed-chunk-31-1.png){fig-alt='자동차의 구동 유형에 대한 두 개의 막대 차트. 첫 번째 그래프에서 막대는\n색상 테두리를 가집니다. 두 번째 그래프에서는 색상으로 채워집니다.\n막대의 높이는 각 drv 카테고리에 속한 자동차의 수에\n해당합니다.' width=384}\n:::\n\n::: {.cell-output-display}\n![](ch05_files/figure-html/unnamed-chunk-31-2.png){fig-alt='자동차의 구동 유형에 대한 두 개의 막대 차트. 첫 번째 그래프에서 막대는\n색상 테두리를 가집니다. 두 번째 그래프에서는 색상으로 채워집니다.\n막대의 높이는 각 drv 카테고리에 속한 자동차의 수에\n해당합니다.' width=384}\n:::\n:::\n\n\nfill 시각 속성을 `class` 같은 다른 변수에 매핑하면 막대가 자동으로 누적(stacked)됩니다. 각 색상 사각형은 `drv`와 `class`의 조합을 나타냅니다.\n\n\n::: {.cell}\n\n```{.r .cell-code}\nggplot(mpg, aes(x = drv, fill = class)) +\n  geom_bar()\n```\n\n::: {.cell-output-display}\n![](ch05_files/figure-html/unnamed-chunk-32-1.png){fig-alt='자동차의 구동 유형에 대한 분할 막대 차트(Segmented bar chart),\n여기서 각 막대는 자동차 class에 대한 색상으로 채워집니다. 막대의 높이는 각 구동\n카테고리의 자동차 수에 해당하며 색상이 칠해진\n세그먼트의 높이는 특정 구동 유형 수준 내에서 특정\nclass 수준을 가진 자동차의 수를 나타냅니다.' width=576}\n:::\n:::\n\n\n누적(stacking)은 `position` 인수로 지정한 **위치 조정(position adjustment)**에 따라 자동으로 이루어집니다. 누적 막대 차트를 원하지 않으면 `\"identity\"`, `\"dodge\"`, `\"fill\"` 가운데 하나를 사용합니다.\n\n1. `position = \"identity\"`는 각 객체를 그래프의 원래 위치에 정확히 배치합니다. 막대가 겹치므로 막대 차트에는 그다지 유용하지 않습니다. 겹침을 보려면 `alpha`를 작은 값으로 설정해 막대를 약간 투명하게 하거나 `fill = NA`로 완전히 투명하게 만들어야 합니다. 항등(identity) 위치 조정은 이를 기본값으로 쓰는 점(points) 같은 2차원 도형에 더 유용합니다.\n\n\n::: {.cell layout-ncol=\"2\"}\n\n```{.r .cell-code}\n# Left\nggplot(mpg, aes(x = drv, fill = class)) +\n  geom_bar(alpha = 1/5, position = \"identity\")\n\n# Right\nggplot(mpg, aes(x = drv, color = class)) +\n  geom_bar(fill = NA, position = \"identity\")\n```\n\n::: {.cell-output-display}\n![](ch05_files/figure-html/unnamed-chunk-33-1.png){fig-alt='자동차의 구동 유형에 대한 분할 막대 차트, 여기서 각 막대는 자동차 class에 대한 색상으로 채워집니다.\n색상이 칠해진\n세그먼트의 높이는 특정 구동 유형 수준 내에서 특정 class\n수준을 가진 자동차의 수를 나타냅니다. 그러나 세그먼트들이 겹칩니다.\n첫 번째 그래프에서 막대는 투명한 색상으로 채워져 있고\n두 번째 그래프에서는 색상으로 윤곽선만 그려져 있습니다.' width=384}\n:::\n\n::: {.cell-output-display}\n![](ch05_files/figure-html/unnamed-chunk-33-2.png){fig-alt='자동차의 구동 유형에 대한 분할 막대 차트, 여기서 각 막대는 자동차 class에 대한 색상으로 채워집니다.\n색상이 칠해진\n세그먼트의 높이는 특정 구동 유형 수준 내에서 특정 class\n수준을 가진 자동차의 수를 나타냅니다. 그러나 세그먼트들이 겹칩니다.\n첫 번째 그래프에서 막대는 투명한 색상으로 채워져 있고\n두 번째 그래프에서는 색상으로 윤곽선만 그려져 있습니다.' width=384}\n:::\n:::\n\n\n2. `position = \"fill\"`은 누적처럼 작동하지만 각 누적 막대 세트의 높이를 같게 만듭니다. 그러면 그룹 간 비율을 쉽게 비교합니다.\n\n3. `position = \"dodge\"`는 겹치는 객체를 나란히 배치합니다. 그러면 개별 값을 쉽게 비교합니다.\n\n\n::: {.cell layout-ncol=\"2\"}\n\n```{.r .cell-code}\n# Left\nggplot(mpg, aes(x = drv, fill = class)) +\n  geom_bar(position = \"fill\")\n\n# Right\nggplot(mpg, aes(x = drv, fill = class)) +\n  geom_bar(position = \"dodge\")\n```\n\n::: {.cell-output-display}\n![](ch05_files/figure-html/unnamed-chunk-34-1.png){fig-alt='왼쪽에는 자동차 구동 유형의 분할 막대 차트, 여기서 각 막대는 class 수준에 대한 색상으로\n채워져 있습니다. 각 막대의 높이는 1이고 색상이 칠해진\n세그먼트의 높이는 특정 구동 유형 내에서 특정 class\n수준을 가진 자동차의 비율을 나타냅니다.\n오른쪽에는 자동차 구동 유형의 회피(dodged) 막대 차트. 회피된 막대들은\n구동 유형의 수준에 따라 그룹화됩니다. 각 그룹 내에서 막대들은 class의\n각 수준을 나타냅니다. 일부 class는 일부 구동 유형 내에 표시되고 다른\n구동 유형 내에는 표시되지 않아 각 그룹 내 막대 수가 다릅니다.\n이러한 막대의 높이는 주어진 수준의 구동 유형 및 class를\n가진 자동차의 수를 나타냅니다.' width=384}\n:::\n\n::: {.cell-output-display}\n![](ch05_files/figure-html/unnamed-chunk-34-2.png){fig-alt='왼쪽에는 자동차 구동 유형의 분할 막대 차트, 여기서 각 막대는 class 수준에 대한 색상으로\n채워져 있습니다. 각 막대의 높이는 1이고 색상이 칠해진\n세그먼트의 높이는 특정 구동 유형 내에서 특정 class\n수준을 가진 자동차의 비율을 나타냅니다.\n오른쪽에는 자동차 구동 유형의 회피(dodged) 막대 차트. 회피된 막대들은\n구동 유형의 수준에 따라 그룹화됩니다. 각 그룹 내에서 막대들은 class의\n각 수준을 나타냅니다. 일부 class는 일부 구동 유형 내에 표시되고 다른\n구동 유형 내에는 표시되지 않아 각 그룹 내 막대 수가 다릅니다.\n이러한 막대의 높이는 주어진 수준의 구동 유형 및 class를\n가진 자동차의 수를 나타냅니다.' width=384}\n:::\n:::\n\n\n막대 차트에는 유용하지 않지만 산점도에는 매우 유용한 조정도 있습니다. 첫 번째 산점도를 다시 떠올려보세요. 데이터셋에는 관측치가 234개인데 그래프에는 점이 126개만 표시됩니다.\n\n\n::: {.cell}\n::: {.cell-output-display}\n![](ch05_files/figure-html/unnamed-chunk-35-1.png){fig-alt='음의 연관성을 보여주는 자동차의 고속도로 연료 효율 대 엔진 크기의\n산점도.' width=576}\n:::\n:::\n\n\n기저(underlying)의 `hwy`와 `displ` 값이 반올림되어 점들이 그리드(grid)에 나타나고 서로 많이 겹칩니다. 이 문제를 오버플로팅(overplotting)이라고 합니다.\n\n이러한 배치(arrangement)는 데이터 분포를 파악하기 어렵게 만듭니다. 데이터 포인트가 그래프 전체에 고르게 퍼져 있나요, 아니면 `hwy`와 `displ`의 특정 조합에 값 109개가 몰려 있나요?\n\n위치 조정을 \"jitter\"로 설정하면 이 그리딩(gridding)을 피합니다. `position = \"jitter\"`는 각 점에 소량의 임의 노이즈(random noise)를 더합니다. 두 점이 같은 양의 노이즈를 받을 가능성이 거의 없어 점들이 넓게 퍼집니다.\n\n\n::: {.cell}\n\n```{.r .cell-code}\nggplot(mpg, aes(x = displ, y = hwy)) +\n  geom_point(position = \"jitter\")\n```\n\n::: {.cell-output-display}\n![](ch05_files/figure-html/unnamed-chunk-36-1.png){fig-alt='자동차의 고속도로 연료 효율 대 엔진 크기의 지터 처리된(Jittered) 산점도.\n그래프은 음의 연관성을 보여줍니다.' width=576}\n:::\n:::\n\n\n무작위성(randomness)을 추가해 그래프을 개선한다는 말이 이상하게 들릴 수 있습니다. 소규모에서는 그래프의 정확성이 떨어지지만 대규모에서는 그래프가 *더* 많은 것을 드러냅니다(revealing).\n\n이 작업은 매우 유용해서 ggplot2가 `geom_point(position = \"jitter\")`의 약어인 `geom_jitter()`를 제공합니다.\n\n위치 조정은 각 조정의 도움말 페이지(`?position_dodge`, `?position_fill`, `?position_identity`, `?position_jitter`, `?position_stack`)에서 자세히 알아보세요.\n\n### 연습 문제\n\n1. 다음 그래프의 문제점은 무엇인가요? 어떻게 개선할 수 있을까요?\n\n\n::: {.cell}\n\n```{.r .cell-code}\nggplot(mpg, aes(x = cty, y = hwy)) +\n  geom_point()\n```\n:::\n\n\n2. 만약 있다면, 이 두 그래프 사이의 차이점은 무엇입니까? 이유는 무엇입니까?\n\n\n::: {.cell}\n\n```{.r .cell-code}\nggplot(mpg, aes(x = displ, y = hwy)) +\n  geom_point()\nggplot(mpg, aes(x = displ, y = hwy)) +\n  geom_point(position = \"identity\")\n```\n:::\n\n\n3. `geom_jitter()`에 전달하는 어떤 매개변수가 지터링(jittering)의 양을 제어합니까?\n\n4. `geom_jitter()`와 `geom_count()`를 비교하고 대조하세요.\n\n5. `geom_boxplot()`의 기본 위치 조정은 무엇입니까? 이를 보여주는 `mpg` 데이터셋의 시각화를 만드세요.\n\n## 좌표계\n\n좌표계(Coordinate systems)는 ggplot2에서 다소 복잡한 부분입니다. 기본값은 x 위치와 y 위치가 독립적으로 작용해 각 점의 위치를 정하는 데카르트 좌표계(Cartesian coordinate system)입니다. 이 밖에 유용한 좌표계가 두 가지 있습니다.\n\n1. `coord_quickmap()`은 지리 지도(geographic maps)의 가로세로 비율(aspect ratio)을 올바르게 설정합니다. ggplot2로 공간 데이터(spatial data)를 그래프할 때 매우 중요합니다.\n\n\n::: {.cell layout-ncol=\"2\"}\n\n```{.r .cell-code}\nnz <- map_data(\"nz\")\n\nggplot(nz, aes(x = long, y = lat, group = group)) +\n  geom_polygon(fill = \"white\", color = \"black\")\n\nggplot(nz, aes(x = long, y = lat, group = group)) +\n  geom_polygon(fill = \"white\", color = \"black\") +\n  coord_quickmap()\n```\n\n::: {.cell-output-display}\n![](ch05_files/figure-html/unnamed-chunk-39-1.png){fig-alt='뉴질랜드 경계의 두 지도. 첫 번째 그래프에서는 가로세로\n비율이 잘못되었고 두 번째 그래프에서는 올바릅니다.' width=288}\n:::\n\n::: {.cell-output-display}\n![](ch05_files/figure-html/unnamed-chunk-39-2.png){fig-alt='뉴질랜드 경계의 두 지도. 첫 번째 그래프에서는 가로세로\n비율이 잘못되었고 두 번째 그래프에서는 올바릅니다.' width=288}\n:::\n:::\n\n\n2. `coord_polar()`는 극좌표(polar coordinates)를 사용합니다. 극좌표에서는 막대 차트와 콕스콤 차트(Coxcomb chart, 나이팅게일의 장미 다이어그램)의 흥미로운 연관성이 드러납니다.\n\n\n::: {.cell layout-ncol=\"2\"}\n\n```{.r .cell-code}\nbar <- ggplot(data = diamonds) +\n  geom_bar(\n    mapping = aes(x = clarity, fill = clarity),\n    show.legend = FALSE,\n    width = 1\n  ) +\n  theme(aspect.ratio = 1)\n\nbar + coord_flip()\nbar + coord_polar()\n```\n\n::: {.cell-output-display}\n![](ch05_files/figure-html/unnamed-chunk-40-1.png){fig-alt='두 개의 그래프이 있습니다. 왼쪽은 다이아몬드 clarity에 대한 막대 차트이고\n오른쪽은 동일한 데이터에 대한 콕스콤 차트입니다.' width=288}\n:::\n\n::: {.cell-output-display}\n![](ch05_files/figure-html/unnamed-chunk-40-2.png){fig-alt='두 개의 그래프이 있습니다. 왼쪽은 다이아몬드 clarity에 대한 막대 차트이고\n오른쪽은 동일한 데이터에 대한 콕스콤 차트입니다.' width=288}\n:::\n:::\n\n\n### 연습 문제\n\n1. `coord_polar()`를 사용하여 누적 막대 차트를 파이 차트(pie chart)로 바꾸세요.\n\n2. `coord_quickmap()`과 `coord_map()`의 차이점은 무엇인가요?\n\n3. 다음 그래프은 도시와 고속도로 mpg의 관계에 대해 무엇을 알려주나요? `coord_fixed()`가 왜 중요할까요? `geom_abline()`은 무슨 역할을 하나요?\n\n\n::: {.cell}\n\n```{.r .cell-code}\nggplot(data = mpg, mapping = aes(x = cty, y = hwy)) +\n  geom_point() +\n  geom_abline() +\n  coord_fixed()\n```\n:::\n\n\n## 그래픽의 레이어 문법\n\n그래프 작성 템플릿에 위치 조정, stats, 좌표계, 분할 처리를 더하면 다음처럼 확장됩니다.\n\n```\nggplot(data = <DATA>) +\n  <GEOM_FUNCTION>(\n     mapping = aes(<MAPPINGS>),\n     stat = <STAT>,\n     position = <POSITION>\n  ) +\n  <COORDINATE_FUNCTION> +\n  <FACET_FUNCTION>\n```\n\n새 템플릿은 대괄호로 묶인 단어 7개를 매개변수로 받습니다. 실제로 ggplot2는 데이터, 매핑, 도형 함수를 제외한 모든 항목에 유용한 기본값을 둡니다. 따라서 그래프를 만들 때 매개변수 7개를 모두 지정하는 경우는 거의 없습니다.\n\n템플릿의 매개변수 7개는 그래프을 구축하는 형식적 시스템(formal system), 즉 그래픽의 문법(grammar of graphics)을 구성합니다. 그래픽의 문법은 어떤 그래프이든 데이터셋, 도형, 매핑 집합, stat, 위치 조정, 좌표계, 분할 체계(faceting scheme), 테마(theme)의 조합으로 고유하게 설명된다는 통찰(insight)에 기반합니다.\n\n작동 방식을 이해하려면 기본적인 그래프을 처음부터 만드는 과정을 생각해 보세요. 데이터셋에서 시작해 표시할 정보로 변환합니다(stat 사용).\n\n다음으로 변환된 데이터의 각 관측치를 나타낼 도형를 선택합니다. 도형의 시각 속성 속성으로 데이터의 변수를 나타내며 각 변수의 값을 시각 속성의 수준(levels)에 매핑합니다.\n\n그 후에는 객체의 위치(그 자체로 시각 속성 속성임)로 `x`와 `y` 변수의 값을 표시할 좌표계를 선택합니다.\n\n이제 완전한 그래프가 만들어집니다. 여기에 좌표계 안에서 도형의 위치를 더 조정하거나(위치 조정) 그래프를 하위 그래프으로 나눌 수도 있습니다(분할). 데이터셋, 도형, 매핑 집합, stat, 위치 조정을 사용하는 레이어를 하나 이상 더해 그래프을 확장할 수도 있습니다.\n\n상상하는 어떤 그래프이든 이 방법으로 만들 수 있습니다. 다시 말해 이 장에서 배운 코드 템플릿으로 수십만 개의 고유한 그래프을 만들 수 있습니다.\n",
    "supporting": [],
    "filters": [
      "rmarkdown/pagebreak.lua"
    ],
    "includes": {},
    "engineDependencies": {},
    "preserve": {},
    "postProcess": true
  }
}